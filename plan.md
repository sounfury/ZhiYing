# 分析流程修复与优化计划

> 状态：已实施并完成模拟回归与浏览器验收（2026-09-12）。
> 范围：失败重试、全书结果一致性、刷新后的任务恢复、进度可见性、后处理性能。未制作人工评测集，不兼容旧数据结构；本轮验证未调用真实模型服务。

## 一、已确认的问题

- 分析中无法重试失败章节；成功章节会继续归一化，但失败章节的证据缺席，最终结果不完整。
- 单章补跑会进行该章归一化和证据验证，但不会重新执行全书总校对。
- 刷新页面后，书籍显示“分析中”，按钮却恢复为“启动分析”，进度连接也没有恢复。
- 前端分析中请求关系图，后端返回 409，前端把正常的状态限制显示为错误。
- 关系归一化与证据验证缺少细分进度；章节串行后处理、需要归一化的关系逐条调用模型，累计等待过长。
- 仅凭现有界面，无法判断当前任务是在等待模型、重试还是已经异常退出；已确认后处理是本次主要耗时阶段，但归一化、验证及重试各自耗时仍需补充测量。

### 实际案例：《悉达多》后处理耗时

以下为 2026-09-12 18:09 左右检查日志与账本得到的快照，不是任务最终完成数据。

- 书籍约 4.8 万字，12 个正文分析章节；任务 ID 对应书籍 `bcb2958e-6863-4fcb-8ae2-17a3dab5fd8e`。
- 17:48:49 开始，17:54:10 完成章节抽取，耗时约 5 分 21 秒；11 章成功、1 章失败，成功账本共抽取 61 条关系。
- 18:08:20 第 7 章后处理落盘，按更新时间已处理 6/11 个成功章节，尚余 5 章；当时还没有进入全书总校对。
- 原因：对需要模型判断的关系逐条发起语义归一化请求，再进行证据验证；当前章全部结束后才开始下一章。61 条关系不等于 61 次归一化请求，完全匹配或证据不能定位的条目会跳过模型调用。
- LLM 请求配置为 120 秒超时、最多重试 2 次，慢请求可能进一步放大串行等待；现有日志不能证明本次哪些请求实际超时或重试。
- 章节完成数在整轮后处理结束后才写回元数据，因此界面仍显示 0/13 已入账，掩盖了已经产生的结果。
- 结论：主要问题是后处理请求过碎、串行执行及进度更新滞后，不应归因于书籍长度或开放关系类型本身。性能修复提升到 P0。

## 二、需要修改的内容

### 1. 统一任务状态与生命周期（优先级 P0）

- [x] 以服务器任务快照作为前端状态依据，不再只依赖页面内的 `running` 布尔值。
- [x] 明确阶段：章节抽取、等待失败章处理、人物合并、关系归一化、证据验证、总校对、势力归纳、完成／部分完成／失败／停止。
- [x] 保存任务 ID、阶段、章节状态、尝试次数、开始时间、最后更新时间、错误摘要和最终结果。
- [x] 后台任务统一处理异常和结束事件，避免任务退出后状态仍停留在“分析中”。
- [x] 区分页面刷新与后端重启：前者恢复连接；后者若已无运行任务，则将遗留任务标记为中断并允许恢复，不能假装后台仍在执行。
- [x] 所有写操作统一经过每本书的任务协调机制，避免重试、校对和人工修改同时写入共享数据。

涉及：`backend/app/core/orchestrator.py`、`backend/app/models/book.py`、`backend/app/storage/filestore.py`、`backend/app/api/analysis.py`。

### 2. 将失败重试接入当前分析任务（P0）

- [x] 失败章节旁提供“重试”，并提供“重试全部失败章”。
- [x] 抽取过程中接受失败章重试请求，将其加入当前队列；其他章节继续处理，成功章节不重复抽取。
- [x] 对同一章节去重，禁止重复排队或同时执行多次重试；界面显示“等待重试／重试中／再次失败”。
- [x] 抽取队列处理完后若仍有失败章，进入“等待失败章处理”，由用户选择重试或“跳过失败章并继续”。
- [x] 等待期间允许停止；刷新后仍能看到失败章节和操作入口。后处理开始后关闭抽取队列，补跑通过后续任务执行。
- [x] 区分失败原因：超时、限流等可进行有上限的退避重试；`data_inspection_failed` 等内容检查拒绝不自动反复提交，不改写原文绕过检查。
- [x] 跳过失败章后允许生成部分结果，明确缺失章节，不显示为全书完整成功。

涉及：编排器、分析／重跑 API、`frontend/src/hooks/useAnalysis.ts`、`frontend/src/components/AnalysisProgress.tsx`、前端 API 和状态类型。

### 3. 补跑后重建全书校对结果（P0）

- [x] 全书分析与单章补跑复用同一套后处理流程，避免两套逻辑行为不一致。
- [x] 补跑成功后先完成该章人物合并、关系归一化和证据验证，再基于全部有效已完成章节重新执行总校对。
- [x] 标记旧校对报告和衍生结果过期；给模型生成的关系补丁记录来源与任务版本，重建时替换旧自动补丁，保留人工修订。
- [x] 梳理人物合并当前会直接改写人物册与账本的问题：保存可重建的抽取结果／合并前快照，避免旧自动合并不可撤销地影响新校对。
- [x] 补跑失败时保留上一次有效结果；采用暂存和发布步骤，避免覆盖到一半的数据成为正式结果。
- [x] 补跑成功但总校对失败时，分别显示章节成功与校对失败，不能直接宣告全部完成。
- [x] 势力归纳等依赖全书结果的数据同步失效；启用自动归纳时重新生成，否则显示待更新。

涉及：编排器、`backend/app/api/edits.py`、`backend/app/core/patch_applier.py`、人物合并逻辑、存储层、账本重跑提示。

### 4. 刷新页面后恢复进度与按钮状态（P0）

- [x] 进入或切换书籍时先读取任务快照，再决定是否加载关系图和显示分析按钮。
- [x] 后端有运行任务时恢复进度订阅，显示当前阶段和停止按钮，禁用启动分析、独立重跑等冲突操作。
- [x] 重试与跳过按钮按服务器阶段开放，不通过简单解除全部按钮禁用来实现。
- [x] SSE 连接先返回当前快照，再发送增量事件；使用事件序号恢复有限历史日志，避免刷新后进度归零。
- [x] 将当前单队列消费改为支持多个订阅者的广播或事件记录，避免多个页面互相抢走进度事件。
- [x] 断线自动重连；切换书籍时关闭旧订阅，忽略旧任务事件与过期请求响应。
- [x] 分析期间暂停图请求；同一本书保留已有图并提示“分析完成后更新”，切换书籍不能残留上一书的图。
- [x] 任务完成后自动刷新书籍、关系图、人物册和账本。竞态情况下收到分析中的 409 时同步任务状态，不直接显示原始错误。

涉及：分析 API、`frontend/src/hooks/useAnalysis.ts`、`frontend/src/hooks/useGraphData.ts`、`frontend/src/state/AppStateProvider.tsx`、顶部按钮与进度组件。

### 5. 补齐后处理进度与诊断信息（P1）

- [x] 抽取阶段显示已处理、成功、失败、排队及运行中章节数；重试不把完成数累加到超过章节总数。
- [x] 归一化与证据验证分别显示当前章节、已处理／总关系数、耗时、失败和待确认数量。
- [x] 总校对显示实际执行步骤，例如读取材料、处理工具请求、等待模型、应用补丁；势力归纳也有独立阶段提示。
- [x] 记录模型请求开始／结束、耗时、重试次数、超时等事件，日志关联任务、章节与阶段；不记录密钥或默认输出整段原文。
- [x] 单次模型调用无法确定完成比例时显示等待状态、耗时和心跳，不伪造百分比。
- [x] 顶部区分阶段进度与全流程完成，避免“章节 100%”被误解为整项任务完成。

涉及：编排器、关系处理管线、归一化器、验证器、LLM 调用层、校对／势力 Agent、前端进度组件。

### 6. 优化耗时，同时保证共享数据一致（P0）

- [x] 先增加阶段与模型调用耗时记录，确定归一化、验证、总校对分别耗时多少。
- [x] 将逐条语义归一化改为按条数及输入长度限制的批量请求；每项携带稳定 ID，校验输出有无遗漏、重复或未知 ID，失败只重试受影响项或批次。
- [x] 证据验证按独立批次有限并发，配置并发上限、请求超时与重试上限，统一汇总后写账本。
- [x] 归一化拆为“基于注册表快照提出解析结果”和“顺序注册／提交”；并发结果提交时重新查重，避免同义关系重复注册或相互覆盖。
- [x] 对完全相同的关系描述复用语义归一化结果；证据结论仍按具体原文上下文验证，不能跨上下文直接复用。
- [x] 支持停止信号与阶段检查点；恢复时按输入及版本是否变化决定复用范围，避免重复已完成的有效工作。
- [x] 保持不确定关系待确认、校验失败不强制确认等准确性约束，不通过取消验证来提速。

涉及：`backend/app/agent/relation_pipeline.py`、`relation_normalizer.py`、`relation_verifier.py`、关系注册表、配置与存储层。

## 三、实施顺序

### 紧急前置项：限制消耗与有效停止（P0）

2026-09-12 用户反馈 token 已耗尽。日志显示本次后处理持续到 18:30:36 才进入总校对，总校对包含 22 个疑点、最多 15 步；前几步读取大量章节账本，后续继续携带累积消息调用模型。实际 token 用量尚无计量，不能从日志推算准确账单。检查还发现总校对循环不检查停止标记，停止 API 不能及时阻止后续调用。本次已终止项目后端进程以阻止继续调度，保留 11 份账本，并把书籍标记为校对失败。

- [x] 优先修复停止信号贯穿所有模型调用循环、重试、批次与新增关系后处理；停止后不再调度请求。明确告知已发往服务商的请求可能仍计费。
- [x] 设置可配置的全任务及分阶段请求数、token 预算与总耗时上限；记录模型返回的 usage，达到预算或余额／额度错误时停止，不能继续自动重试。
- [x] 总校对只处理必要疑点，按相关章节和证据提供最小上下文；避免反复读取整份账本、持续累积长工具结果；对已验证证据避免无理由重复核查。
- [x] 对批量与并发优化同时验收 token 用量和请求数；并发只能缩短等待，不能替代成本控制。
- [x] 在以上限制和停止行为验证通过前，不自动发起真实模型全书重跑；开发验证使用模拟模型。

1. 任务快照、生命周期和事件订阅；修复刷新后的状态、按钮与 409。
2. 补齐后处理计数和调用耗时日志，优先实施批量归一化、相同描述复用、验证有限并发与注册表统一提交。
3. 失败章重试队列和等待／跳过流程。
4. 补跑后的全书校对、结果失效与安全发布。
5. 完善总校对等阶段的细分日志、恢复检查点与性能测量。
6. 完成回归验证与使用说明。

## 四、验收标准

- [x] 分析中刷新页面、打开第二个页面，都能看到同一任务的阶段与失败章节，不出现可重复启动的按钮或读图 409 红条。
- [x] 某章失败时其他章继续处理；重试成功后该章参与统一后处理，总章数不被重复计算。
- [x] 最后一章失败也能进入等待处理状态；重复点击重试不重复执行；等待期间停止能正常结束任务。
- [x] 跳过失败章后结果明确标为部分完成；之后补跑成功触发覆盖全部有效章节的总校对。
- [x] 旧自动补丁不与新校对结果重复叠加，人工修订保留；补跑或发布失败不会破坏上一份有效结果。
- [x] 刷新、断线重连及切换书籍不丢失当前状态、不串日志；后端异常或重启不会留下无法操作的永久“分析中”。
- [x] 长时间模型调用可见当前阶段、耗时与等待状态；超时和重试均有明确反馈。
- [x] 并发归一化不会生成重复注册项，验证请求数量受限，停止后不继续调度新的请求。
- [x] 用固定模拟输入验证批量归一化减少请求数、相同描述可复用、各项输出正确对应；使用可控延迟验证有限并发减少串行等待，不取消证据验证。记录优化前后的请求数与阶段耗时，不将模拟测试提速直接当作真实服务商耗时承诺。
- [x] 使用模拟模型覆盖成功、失败、超时、并发与恢复路径；运行相关后端测试及前端构建，并验证关键浏览器交互。不新建人工评测集。

### 实施与验证结果（2026-09-12）

- 后端全量回归：`123 passed`。测试环境显式设置 `LLM_API_KEY=''`，未发起真实模型调用。
- 前端：`tsc -b && vite build` 通过；仅保留既有的 Vite 大 chunk 提示，不影响本轮功能验收。
- 固定模拟输入：20 条完全相同的开放关系描述只产生 1 个归一化模型候选／1 次模拟调用，20 条关系最终复用同一 predicate。
- 可控延迟并发：8 条关系、batch size=2 时共 4 个验证批次；全局并发上限为 2，测试确认 `max_active == 2`，并在每批模拟延迟 80ms 的条件下通过 `<0.29s` 的管线验收阈值（串行仅延迟下界约 0.32s）。该结果只证明有限并发生效，不代表真实服务商耗时。
- 恢复验证：后端重启后，正文 SHA-256 未变化的已完成章节复用原始抽取快照；正文变化时自动失效并重新抽取。
- 浏览器隔离验收：刷新后恢复同一运行任务和停止按钮；失败章显示单章重试／跳过；重试后仍失败会回到等待态；切换书籍不会串日志或旧运行状态；验证过程中浏览器控制台无错误。
- 成本／停止专项：额度错误只调用 1 次且不重试；重试等待中停止不会再发下一请求；token 预算到达后停止；长请求会发送心跳并显示等待耗时。

## 五、本轮不包含

- 人工评测集与模型准确率结论。
- 为旧版账本、旧关系结构编写迁移兼容层。
- 自动切换服务商或绕过内容检查。
- 无证据支持的提速比例承诺。
