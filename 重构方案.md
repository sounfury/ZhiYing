# ZhiYing 关系归一化与全书 Reconcile 重构方案

> 日期：2026-09-12  
> 目标：彻底替换当前“关系归一化 + Suspect + Tool-calling Reconcile Agent”中的高开销路径。  
> 核心原则：**确定性代码负责发现、去重、聚类、校验和应用；LLM 只负责真正无法由规则安全裁决的语义歧义。**  
> 预期结果：短篇小说（以《悉达多》为基准）后处理阶段正常情况下只发生 **1～3 次模型调用**，不再出现数十轮 Agent 循环和百万级重复输入 Token。

---

## 1. 结论先行

当前实现需要重构，而不是继续给 `reconcile_agent.py` 增加限流、截断和重试规则。

现有代码已经补上了停止、预算、超时、批处理和历史裁剪，这些措施能防止系统再次失控，但它们属于“安全护栏”，并没有改变核心算法：

1. 关系归一化仍然是按章进入 `normalize_relations()`；
2. `SuspectsGenerator` 仍然把大量原子记录直接转成 suspect；
3. 最终一致性处理仍然进入一个自由探索式 Tool-calling Agent；
4. Agent 每一轮都可能重新发送初始上下文和最近工具结果；
5. 一个本质上只有几十条关系的数据集合，被转换成了多轮长上下文对话。

本方案的目标是直接取消这种结构。

新的主流程应改为：

```text
章节抽取结果
  ↓
确定性预处理 / 证据定位
  ↓
全书关系候选去重与聚类
  ↓
安全规则直接归一化
  ↓
少量“语义歧义簇” → LLM 批量裁决
  ↓
确定性证据校验 / 少量歧义证据 → LLM 批量裁决
  ↓
构建全书规范事实表
  ↓
确定性冲突编译与根因聚类
  ↓
真正无法确定的 ReconcileCase → LLM 一次性批量裁决
  ↓
服务端验证 Patch
  ↓
事务性发布
```

**不再存在“模型自己决定查哪个工具、查完再思考、再查、再思考”的循环。**

---

## 2. 当前性能为什么会差到几百万 Token

### 2.1 22 个 suspect 并不等于 22 个真实问题

当前 `backend/app/core/suspects.py` 中：

- `detect_cast_conflicts()` 对人物做两两扫描；
- `detect_relation_conflicts()` 对相同 predicate 的反向断言生成复查项；
- `detect_missing_evidence()` 会对 **每一条 `status == "pending"` 的关系直接生成一个 `MissingEvidence`**。

因此：

```text
22 suspects
≠ 22 个独立错误
≠ 22 个必须单独分析的问题
```

很可能只是：

```text
同一人物对 / 同一关系语义 / 同一根因
在多个章节中留下了多条 pending 记录
→ 被逐条展开成多个 suspect
```

这一步没有做“根因聚类”。

### 2.2 当前 Reconcile 是自由探索式 Agent

`backend/app/agent/reconcile_agent.py` 当前流程为：

```text
构造全书 prompt
→ LLM
→ tool calls
→ tool results
→ LLM
→ tool calls
→ ...
→ submit_reconciliation
```

配置中：

- `MAX_RECONCILE_STEPS` 默认 15；
- `RECONCILE_MAX_LLM_REQUESTS` 当前安全上限 12。

虽然现在已经限制了历史和工具结果长度，但算法仍然允许模型反复探索。

这对于“几十条规范化关系”的问题是过度设计。

### 2.3 重复上下文导致 Token 累加，而不是只计算一次

假设每轮请求上下文分别为：

```text
30k → 50k → 70k → 90k → 110k → ...
```

总输入不是最后一次的 110k，而是：

```text
30k + 50k + 70k + 90k + 110k + ...
```

因此几十轮调用很容易累计到数百万 Token。

最终输出可能只有十几个人物和几十条关系，但模型和程序之间重复搬运了大量相同信息。

### 2.4 归一化仍然受“章节边界”影响

当前 `enrich_ledgers()` 虽然共享一个 relation registry，并且已经支持任务级 cache，但仍是：

```text
for chapter:
    normalize_relations(chapter.relations)
```

也就是说，模型候选组织仍然以章节为主要单位，而不是以“全书唯一语义簇”为单位。

更合理的单位应该是：

```text
全书唯一 RelationSemanticCluster
```

例如 12 章里出现 18 次“朋友 / 友人 / 挚友”类描述，不应该因为分布在 12 章就进入多轮模型流程。

---

## 3. 重构目标

### 3.1 必须达到

1. **取消最终自由探索式 Reconcile Agent。**
2. 相同关系语义在全书范围内先去重，再决定是否调用模型。
3. LLM 不再拥有“无限调查”的控制权，只接收服务端已经准备好的有限候选。
4. LLM 输出必须是严格结构化、带稳定 ID 的批量裁决结果。
5. 所有新增 predicate 仍由服务端生成，模型不得自造主键。
6. 所有 Patch 在发布前由服务端做完整不变量校验。
7. 模型失败时 fail-closed：保留 pending / todo，而不是继续自我探索。
8. 单章重跑仍保持现有 staging + transaction + manual edit replay 语义。
9. 不降低当前证据安全性：模型不能凭空把没有原文证据的关系变成 confirmed。

### 3.2 明确不做

本轮不做：

- 不引入向量数据库；
- 不为关系类型建立重量级知识图谱本体；
- 不做跨书自动共享 learned registry；
- 不依赖旧数据结构兼容；
- 不让模型自动修正文全文；
- 不让 Reconcile 再次读取整本小说。

---

## 4. 新架构总览

建议拆成 6 层。

```text
A. Relation Preprocessor
B. Relation Cluster Compiler
C. Semantic Resolver
D. Evidence Resolver
E. Conflict Compiler
F. Batch Reconcile Resolver
```

### A. Relation Preprocessor —— 完全确定性

输入：所有章节原始抽取关系。

职责：

1. 证据 quote 精确定位；
2. 标准化字符串空白、全半角、常见标点；
3. 无向关系人物对排序；
4. 保留原始 label / definition / raw_relation；
5. 生成稳定的结构签名；
6. 明确哪些记录根本不需要 LLM。

建议增加：

```python
RelationFingerprint = {
    "directed": bool,
    "subject_role": str,
    "object_role": str,
    "category": str,
    "definition": str,
}
```

这里的 fingerprint 只能用于“完全安全”的相等判断，不能凭字符串相似度擅自合并语义。

### B. Relation Cluster Compiler —— 全书级去重

新增全书级候选编译器：

```text
所有 chapter relations
→ exact resolved
→ registry alias resolved
→ duplicate clusters
→ ambiguous semantic clusters
```

建议数据结构：

```python
class RelationSemanticCluster:
    cluster_id: str
    member_relation_ids: list[str]
    representative: RelationDescriptor
    observed_labels: list[str]
    observed_raw_relations: list[str]
    chapter_ids: list[int]
    candidate_predicates: list[str]
```

核心原则：

> 模型处理的是“唯一语义簇”，不是每一条 relation occurrence。

例如全书 20 条完全相同描述：

```text
20 occurrences
→ 1 cluster
→ 最多 1 次语义裁决
→ 结果广播给 20 条记录
```

### C. Semantic Resolver —— LLM 只裁决歧义簇

替换目前 `relation_normalizer.py` 的模型部分。

LLM 输入不再携带整个 registry，而是每个 cluster 只提供：

- cluster 描述；
- 出现的 labels / raw descriptions；
- 通过确定性候选检索得到的少量 registry candidates；
- 稳定 `cluster_id`。

候选检索优先使用：

1. descriptor exact match；
2. registry aliases exact match；
3. label exact match；
4. category + directed + roles 过滤；
5. 最后才把有限候选交给模型比较定义。

模型只能返回：

```python
class SemanticDecision:
    cluster_id: str
    action: Literal["existing", "new", "unresolved"]
    predicate: str | None
    reverse: bool
    reason: str
```

约束：

- `existing` 只能引用输入候选里的 predicate；
- `new` 不允许模型生成 predicate；
- `new` 由服务端 `register()` 生成稳定 predicate；
- 任意非法输出直接 unresolved；
- 不进行第二轮“你再想想”。

#### 批处理策略

按 cluster 数和字符数切 batch，而不是按章节切 batch。

推荐默认：

```text
SEMANTIC_BATCH_SIZE=24
SEMANTIC_BATCH_CHARS=24_000
SEMANTIC_MAX_REQUESTS_SHORT=2
```

对《悉达多》这种规模，正常目标：**0～1 次**。

### D. Evidence Resolver —— 证据校验从“逐章批次”改为“全书待判定集合”

当前 quote 是否存在已经可以确定性校验，这部分继续保留。

证据校验拆成三档：

#### D1. 直接拒绝

- quote 不存在；
- quote 非唯一且无法定位；
- person id 不存在；
- relation descriptor 结构非法。

不调用模型。

#### D2. 直接确认的安全场景

仅允许非常保守的规则，例如：

- 已有人工确认覆盖；
- 已存在完全相同 `relation_id + evidence hash` 的确认结果；
- 单章重跑且证据文本未变化，可复用同一证据 hash 的 verdict。

不建议通过关键词规则自动做复杂语义确认。

#### D3. 需要语义判断

把所有仍需判断的关系收集成全书级 `EvidenceCase`：

```python
class EvidenceCase:
    case_id: str
    relation_id: str
    person_a_name: str
    person_b_name: str
    normalized_relation: str
    evidence_quote: str
    local_context: str
```

一次批量送模型判断：

```text
confirmed / rejected / pending
```

模型只看局部 evidence window，不读取整章，更不读取整本书。

目标：短篇全书证据复核 **0～1 次模型请求**；数据多时按 case 数切有限批次。

---

## 5. 用 Conflict Compiler 替换 SuspectsGenerator

这是本次重构最关键的一步之一。

当前问题是：

```text
原子异常记录
→ suspect
```

新设计必须变成：

```text
原子异常记录
→ 确定性归并
→ root-cause case
```

新增：

```text
backend/app/core/conflict_compiler.py
```

输出：

```python
class ReconcileCase:
    case_id: str
    case_type: str
    person_ids: list[str]
    predicate_ids: list[str]
    relation_ids: list[str]
    chapter_ids: list[int]
    evidence_snippets: list[str]
    deterministic_facts: dict
    allowed_actions: list[str]
```

### 5.1 人物冲突：按连通分量聚类，而不是 pair suspect

例如：

```text
A 的 alias 命中 B
B 的 alias 命中 C
A 与 C 又共享别名
```

旧方式可能产生：

```text
A-B
A-C
B-C
```

新方式：

```text
alias graph
→ connected component {A,B,C}
→ 1 个 identity case
```

这样一个根因只产生一个 ReconcileCase。

### 5.2 关系冲突：以规范事实键聚类

建议建立内部事实键：

```text
(person_pair, predicate, direction)
```

对于无向关系，person pair 强制排序。

同一事实在多个章节出现：

```text
不是多个 suspect
而是一个 fact，带多个 evidence occurrences
```

### 5.3 pending evidence 不再自动升级为 Reconcile 问题

这是必须修改的逻辑。

`status == pending` 只代表：

> 当前证据尚不能自动确认。

它不代表：

> 全书事实发生冲突，必须让 Reconcile Agent 调查。

因此新逻辑：

- 普通 pending → 保留为 pending / review queue；
- 只有 pending 与已确认事实形成明确冲突时，才进入 ReconcileCase；
- 多条同根因 pending 先合并，再进入一个 case。

这一项本身就会显著减少“22 个疑点”式膨胀。

---

## 6. 取消 Tool-calling Reconcile Agent

计划废弃：

```text
backend/app/agent/reconcile_agent.py
```

至少废弃其“自由工具循环”执行模式。

替换为：

```text
backend/app/agent/reconcile_resolver.py
```

### 6.1 输入必须在调用模型前准备完整

模型一次拿到：

```text
ReconcileCase[]
+ 每个 case 的局部证据
+ 当前人物摘要
+ 允许动作
```

禁止模型主动：

- 读取整章；
- 搜索整本书；
- 再查 ledger；
- 再查 cast；
- 调工具；
- 自己决定扩大调查范围。

如果编译器提供的信息不足，模型只能返回：

```text
unresolved / todo
```

而不是启动第二轮调查。

### 6.2 输出为批量结构化 Decision

建议：

```python
class ReconcileDecision:
    case_id: str
    action: Literal[
        "keep",
        "merge_persons",
        "add_alias",
        "remove_relation",
        "replace_relation",
        "todo",
    ]
    target_ids: list[str]
    reason: str
```

服务端把这些 Decision 编译为现有 `ReconcilePatch`。

### 6.3 模型无权直接修改数据库状态

模型只给 decision。

随后由：

```text
DecisionValidator
→ PatchCompiler
→ PatchApplier
```

完成确定性应用。

任何以下情况直接拒绝整个对应 case：

- 未知 case_id；
- 未知 person_id；
- 未知 relation_id；
- 删除不存在 relation；
- merge 自环；
- 修改证据 quote；
- 引入未验证关系；
- predicate 不在 registry；
- 模型输出 case 未授权的 action。

---

## 7. 新的完整数据流

```text
1. Chapter Agent 抽取
   ↓
2. 保存 raw extraction ledger
   ↓
3. RelationPreprocessor
   - quote locate
   - structural validation
   - stable fingerprints
   ↓
4. RelationClusterCompiler（全书）
   - exact dedupe
   - registry alias match
   - semantic clusters
   ↓
5. SemanticResolver
   - 只处理 ambiguous clusters
   - 0～N 个有限 batch
   ↓
6. EvidenceResolver
   - deterministic gate
   - 只把真正需要语义判断的 case 批量给模型
   ↓
7. CanonicalFactCompiler
   - 把 occurrence 聚合为 canonical facts
   ↓
8. ConflictCompiler
   - identity connected components
   - relation contradiction groups
   - pending root-cause groups
   ↓
9. 无 conflict case？
   ├─ 是 → 直接结束
   └─ 否 → BatchReconcileResolver
             - 无 tools
             - 结构化批量输出
   ↓
10. DecisionValidator
   ↓
11. PatchCompiler
   ↓
12. PatchApplier（现有事务语义）
   ↓
13. 发布结果
```

---

## 8. 模块调整建议

### 保留并强化

```text
backend/app/core/relation_registry.py
backend/app/core/evidence.py
backend/app/core/patch_applier.py
backend/app/core/reconcile_service.py      # 改为编排器
backend/app/agent/llm.py                   # 保留预算/停止/统计
backend/app/storage/filestore.py           # 保留 staging / snapshot
```

### 重写

```text
backend/app/agent/relation_normalizer.py
backend/app/agent/relation_pipeline.py
backend/app/core/suspects.py
backend/app/agent/reconcile_agent.py
```

### 新增

```text
backend/app/core/relation_preprocessor.py
backend/app/core/relation_clusters.py
backend/app/agent/semantic_resolver.py
backend/app/core/evidence_cases.py
backend/app/agent/evidence_resolver.py
backend/app/core/canonical_facts.py
backend/app/core/conflict_compiler.py
backend/app/agent/reconcile_resolver.py
backend/app/core/reconcile_validator.py
backend/app/core/reconcile_patch_compiler.py
```

### 最终删除 / 禁用

Reconcile 路径不再依赖：

```text
make_reconcile_tools()
ReconcileToolContext
bind_tools(...)
max_reconcile_steps
reconcile_history_messages
reconcile_tool_result_chars
```

这些配置可在迁移完成后移除。

---

## 9. 调用次数与 Token 预算

### 9.1 短篇基准：《悉达多》

后处理阶段，不含章节正文抽取：

| 阶段 | 正常目标 | 硬上限 |
|---|---:|---:|
| Relation semantic normalization | 0～1 次 | 2 次 |
| Evidence semantic verification | 0～1 次 | 2 次 |
| Final Reconcile | 0～1 次 | 2 次 |
| **后处理总计** | **1～3 次** | **6 次** |

正常情况下 Final Reconcile 应只有 **0 或 1 次**。

如果一次批量裁决失败：

- 不自动进入 Agent loop；
- 最多允许一次协议级重试；
- 再失败则将对应 case 标记 unresolved。

### 9.2 Token 目标

《悉达多》后处理建议验收阈值：

```text
总输入 Token：< 100k
Final Reconcile 输入 Token：< 30k
Final Reconcile 输出 Token：< 8k
```

目标值不是为了卡模型，而是作为架构异常报警线。

如果《悉达多》的后处理再次出现：

```text
> 200k input tokens
或
> 6 requests
```

应直接视为性能回归，不应接受。

### 9.3 长书不按章节数线性增加 Reconcile 调用

模型请求数量应该主要取决于：

```text
unique ambiguous semantic clusters
+ unique evidence cases
+ unique reconcile root-cause cases
```

而不是：

```text
chapter_count
或
raw relation occurrence count
```

推荐 batch 公式：

```text
semantic_requests  = ceil(ambiguous_clusters / 24)
evidence_requests  = ceil(evidence_cases / 32)
reconcile_requests = ceil(reconcile_cases / 24)
```

同时受字符上限约束。

---

## 10. 性能验收标准

### P0：必须通过

#### 《悉达多》

- Final Reconcile 正常 ≤ 1 次模型调用；
- Final Reconcile 硬上限 ≤ 2 次；
- 整个关系后处理正常 ≤ 3 次模型调用；
- 整个关系后处理硬上限 ≤ 6 次；
- 后处理总输入 Token < 100k；
- 不存在 tool-calling loop；
- 不存在“每个 pending relation 生成一个 Reconcile 调查轮次”；
- 相同语义关系跨章只裁决一次；
- 相同 evidence hash 不重复验证；
- 模型失败后不会扩大上下文重试。

### P1：耗时

在模型服务正常延迟条件下：

```text
《悉达多》后处理 p50 < 30s
《悉达多》后处理 p95 < 90s
```

这里不把上游服务极端排队时间算成算法性能，但必须保留现有 120s 单请求超时和任务预算。

### P1：正确性

与重构前人工确认的黄金集比较：

- confirmed relation precision 不下降；
- 人物错误合并数 = 0；
- 不允许不同语义只因 label 相似而合并；
- 方向性关系不得错误反转；
- 无证据 relation 不得自动进入 confirmed；
- manual edits 必须完整保留；
- 单章 rerun 后全书结果必须可重复构建。

---

## 11. 需要新增的测试

### 11.1 聚类测试

1. 20 条完全相同 relation occurrence → 1 个 cluster；
2. 跨 12 章的同一 descriptor → 1 个 cluster；
3. 同 label、不同 definition → 不得合并；
4. directed 与 undirected → 不得合并；
5. subject/object role 不等价 → 不得合并。

### 11.2 Alias / identity 冲突测试

构造：

```text
A ↔ B
B ↔ C
A ↔ C
```

旧系统可能生成 3 suspects；新系统必须生成 1 个 identity ReconcileCase。

### 11.3 pending 测试

20 条普通 pending：

```text
没有相互冲突
```

必须：

```text
0 个 ReconcileCase
20 条 pending review items
```

不能再触发 20 个 suspect。

### 11.4 Reconcile 批量测试

24 个 case：

```text
1 个模型请求
```

25～48 个：

```text
2 个模型请求
```

且第二批不得携带第一批的模型对话历史。

### 11.5 Token 回归测试

Fake LLM 记录每次 messages 字符量和 token usage。

测试必须断言：

```text
后续请求不会因为前面请求而单调携带累积历史
```

### 11.6 真实书籍离线评测

使用现有三本：

```text
短：《悉达多》
中：《画像》
长：《白痴》
```

记录：

- raw relation count；
- unique clusters；
- model-bound clusters；
- evidence cases；
- reconcile cases；
- 每阶段 requests；
- 每阶段 input/output tokens；
- wall time；
- 最终人物数；
- 最终关系数；
- pending 数；
- 人工正确率。

这些数据必须成为以后每次重构的 baseline。

---

## 12. 实施顺序

### Phase 0 —— 固化基线，不改行为

新增性能统计输出：

```text
relation_occurrences
unique_descriptors
normalization_model_candidates
normalization_requests
verification_requests
suspect_count
reconcile_requests
input_tokens_by_phase
output_tokens_by_phase
```

对《悉达多》跑一次模拟 / mock 基线，不先调用真实模型。

**完成条件：能明确看到每一阶段到底为什么产生模型调用。**

---

### Phase 1 —— 全书 Relation Cluster Compiler

先只改归一化入口，不碰最终 Reconcile。

实现：

```text
全书 relation occurrences
→ 全书级 cluster
→ 一次批量 semantic resolution
→ 广播结果到各 ledger
```

保留当前 `RelationRegistry` 和服务端 `register()`。

**完成条件：相同语义不再因章节拆分重复调用。**

---

### Phase 2 —— 全书 Evidence Resolver

把当前按章 evidence verification 改为：

```text
collect all semantic evidence cases
→ batch verify
→ distribute verdicts
```

局部上下文窗口按 evidence quote 附近截取。

**完成条件：《悉达多》 evidence verification 正常 1 次请求。**

---

### Phase 3 —— Conflict Compiler

新增：

```text
canonical facts
identity components
relation conflict groups
pending review queue
```

暂时仍可把 `ReconcileCase` 转成旧 `SuspectList` 喂给旧 Agent，方便逐步替换。

**完成条件：旧的 22 个原子疑点能明显压缩为少量 root-cause cases。**

---

### Phase 4 —— Batch Reconcile Resolver

实现无工具版：

```text
ReconcileCase[]
→ one structured LLM request
→ ReconcileDecision[]
→ validator
→ patch compiler
→ PatchApplier
```

旧 `run_reconcile_agent()` 保留 feature flag 回滚。

配置建议：

```text
RECONCILE_MODE=batch
```

可选：

```text
RECONCILE_MODE=legacy_agent
```

仅用于回归对照，不作为默认路径。

**完成条件：正常流程不再调用任何 Reconcile tool。**

---

### Phase 5 —— 真实评测 + 删除旧架构

在三本测试书上跑评测集。

达到正确性和成本阈值后：

删除：

```text
legacy Reconcile Agent loop
ReconcileToolContext
make_reconcile_tools
MAX_RECONCILE_STEPS
RECONCILE_HISTORY_MESSAGES
RECONCILE_TOOL_RESULT_CHARS
```

保留全局任务级 LLM budget 作为最后一道安全保护。

---

## 13. 回滚策略

重构期间必须保留 feature flag：

```text
RELATION_PIPELINE_VERSION=v2
RECONCILE_MODE=batch
```

出现严重错误可切回：

```text
RELATION_PIPELINE_VERSION=v1
RECONCILE_MODE=legacy_agent
```

但旧路径只作为短期迁移保险，不继续开发新功能。

所有新路径在正式 publish 前继续使用现有 staging 机制，因此：

- 解析失败；
- 模型失败；
- validator 失败；
- patch apply 失败；

都不能污染当前已发布图谱。

---

## 14. 可观测性必须成为正式接口

前端和日志至少暴露：

```text
关系 occurrence 数
唯一 semantic cluster 数
规则直接解决数
进入 LLM 的 cluster 数
EvidenceCase 数
ReconcileCase 数
每阶段模型调用次数
每阶段 input/output tokens
每阶段耗时
```

理想日志：

```text
Relations: 54 occurrences
→ 17 semantic clusters
→ 13 resolved deterministically
→ 4 model candidates
→ 1 normalization request

Evidence: 54 occurrences
→ 18 reused/exact
→ 36 semantic cases
→ 1 verification request

Reconcile: 21 raw anomalies
→ 5 root-cause cases
→ 1 request
→ 3 patches + 2 todos
```

看到这种日志才能真正判断性能是否正常，而不是只看“卡没卡住”。

---

## 15. 关于《悉达多》的预期

以目前已经观察到的数据规模，这本书不应该出现几十次最终模型调用。

重构后合理表现应接近：

```text
几十条 relation occurrences
↓
十几个 unique semantic clusters
↓
大部分规则 / registry 直接解决
↓
少量 cluster 一次批量归一化
↓
证据一次批量确认
↓
若存在 20+ raw anomalies，先聚成少量 root-cause cases
↓
一次 Final Reconcile
```

也就是说：

> **百万 Token 不是《悉达多》这个任务的合理成本，而是旧算法重复搬运上下文的成本。**

本次重构的最终评价标准不是“不会卡一小时”，而是：

> **为什么一个只有十几个人物、几十条关系的短篇，需要超过个位数模型调用？**

如果回答不了，就说明架构仍然有问题。

---

## 16. 最终验收定义

只有同时满足以下条件，才算这次重构完成：

- [ ] Reconcile 不再使用 tool-calling loop；
- [ ] pending relation 不再逐条转成 Reconcile suspect；
- [ ] 全书关系先 cluster，再调用模型；
- [ ] 相同语义跨章只做一次 semantic decision；
- [ ] Reconcile 输入只包含 root-cause cases 和局部证据；
- [ ] LLM 不能主动扩大调查范围；
- [ ] LLM 输出只能引用服务端提供的稳定 ID；
- [ ] Patch 应用前存在严格 validator；
- [ ] 《悉达多》后处理正常 ≤ 3 次模型请求；
- [ ] 《悉达多》后处理硬上限 ≤ 6 次模型请求；
- [ ] 《悉达多》后处理 input tokens < 100k；
- [ ] 人物错误合并数 = 0；
- [ ] confirmed relation precision 不低于重构前黄金集；
- [ ] 单章 rerun、人工修改、事务回滚行为全部通过回归测试；
- [ ] 三本测试书均生成可对比性能报告。

---

## 17. 推荐的第一刀

不要先改 Prompt。

第一步直接把：

```python
detect_missing_evidence()
```

从：

```text
每个 pending relation → 一个 MissingEvidence suspect
```

改成：

```text
pending relation → pending review pool
只有存在真正全书冲突时 → ReconcileCase
```

同时实现全书级 `RelationSemanticCluster`。

这两项做完以后，即使暂时保留旧 Reconcile Agent，suspect 数和归一化调用次数都会先明显下降。

随后再彻底移除自由 Tool-calling Reconcile loop。

---

**最终目标：把“Agent 自己调查整本书”改成“程序准备好有限问题，模型一次批量判卷”。**