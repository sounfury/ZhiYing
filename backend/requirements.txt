# ── Web framework ──
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9,<1.0

# ── Data validation ──
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# ── LLM (LangChain — 仅 Agent 运行时，不做书级编排) ──
langchain-core>=0.2,<0.4
langchain-openai>=0.1,<0.3

# ── EPUB parsing ──
ebooklib>=0.18
beautifulsoup4>=4.12,<5.0
lxml>=5.1

# ── Storage ──
aiosqlite>=0.20,<1.0

# ── HTTP client (LangChain 依赖，显式声明；socks 供 ALL_PROXY=socks5 环境) ──
httpx[socks]>=0.27,<1.0