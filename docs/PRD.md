# ZhiYing 产品需求文档（PRD）

| 项 | 内容 |
|----|------|
| 产品名 | **ZhiYing** |
| 一句话 | 上传电子书，自动生成准确、可读、可解释的人物关系图 |
| 文档性质 | PRD v0.9 |
| 日期 | 2026-09-24 |
| 状态 | 产品目标 SSOT |
| 本次修订 | 确认团体分区与有向关系均纳入 Kotlin 重构首批交付；失败章节与结果发布策略仍待确认 |

---

## 1. 背景与问题

读中长篇小说时，人物多、称呼乱、关系随章节变化。读者往往只能：

- 手工画思维导图
- 把摘要丢给通用 AI 生成关系图
- 网上搜别人画的图（易剧透、不准）

市面缺少一套顺畅的体验：

> **上传书 → 自动出人物关系 → 可读、可解释 → 浏览与导出**

按阅读进度防剧透作为二期候选特色功能，当前不作为核心闭环与验收要求。

现有开源方案大多偏重状态机和多层门控，工程重、对模型约束极死。ZhiYing 走更轻的路线：过程交给 Agent 灵活处理，出口用结构和规则兜住质量。

---

## 2. 产品目标

### 2.1 要做成什么样

1. **主输入：EPUB**（主流电子书格式；TXT 仅作后续兼容）
2. **先建人名册，再抽关系**——减少同一个人裂成多个节点
3. **按章分析并汇总**；按阅读进度查看的防剧透功能后置到二期候选
4. **关系多标签**——同一对人物可同时具备多种关系类型
5. **软关系兜底**——先找硬 / 中关系；无此类关系但有实际交流依据时考虑软关系。之前已有的软关系保留，默认可被强关系折叠；硬关系之间互不覆盖
6. **证据可解释**——关系尽量带来源章节与原文短句
7. **出图可过滤**——默认可读，不靠蜘蛛网硬撑「全量真实」
8. **势力 = 团体聚合**——学校、教会、组织、家族、阵营等均可，与关系边正交（见 §3.1）
9. **百人级大图以势力分区为主**——100+ 人仍要一张可读大图时，按势力分块最有效（见 §4.5）；过滤和聚焦是配套手段，不能替代分区

### 2.2 明确不做（现阶段）

- 不做成通用写作 IDE 或网文诊断台
- 不追求一次塞进整本长上下文「魔法出图」
- 不复刻现有方案的重状态机与多层裁决
- 不把 PDF / 扫描版放进首期
- 不默认做社交图谱社区（版权与剧透风险）
- 当前不做按阅读进度防剧透；二期是否作为特色功能实施，另行确定
- 本期不做人工纠错，不要求用户判断人物身份、审核关系或修正团体归属
- 本期只做基础证据模式，不做加强模式及模式切换

---

## 3. 目标用户

| 用户 | 定位与诉求 |
|------|------|
| 尚未读过这本书的用户 | **核心用户**。无需预先了解情节与人物，即可得到系统自动生成、可理解的关系图 |
| 已读过书、主要为了画图的读者，读书会 / 自媒体 | 非核心用户，可浏览与导出；本期不围绕其手动校图诉求设计流程 |

核心用户缺少判断结果对错的知识。若系统必须依赖用户合并人物、补关系或逐项审核才能产出可用图，就没有完成核心任务。本期自动完成认人、关系判断、补查与出图；质量问题不能通过增加人工纠错步骤转交给用户。

未读过书的用户是当前定位；防剧透仍按既定决定后置，不因此重新纳入本期。
### 3.1 术语：什么叫「势力」

PRD 中的**「势力」**不是狭义的政治派系或正邪对立，而是：

> **人物所属的团体聚合（affiliation group）——一群人因共同机构、场景、组织身份或稳定归属而聚在一起。**

| 算势力 | 例子 |
|--------|------|
| 学校 / 年级阶段 | 小学、中学、大学 |
| 宗教 / 教会 | 某教堂、修会、神职圈子 |
| 组织 / 机构 | 公司、门派、军队、学院 |
| 家族 / 房支（作团体时） | 贾府、某世家（血缘细节仍由关系边表达） |
| 政治 / 对抗阵营 | 皇后党、叛军——只是势力的子类，不是定义本身 |
| 叙事世界 / 生活圈 | 成长小说里「某阶段的生活世界」 |

| 不算势力 | 说明 |
|----------|------|
| 两人间的关系类型 | 朋友、亲子、师徒 → 属于边 / 标签（见 §5.5） |
| 亲疏强弱 | 与主角的远近 → 属于边权重，不是团体 |
| 单次同场 | 无稳定团体身份，不自动升格为势力；能否作为软关系兜底还要看实际交流依据（§5.5） |

**与关系正交：**

- **势力**描述节点归属：人落在哪个团体块（可多归属）
- **关系**描述边：人与人之间的类型（朋友 / 师徒 / 亲子…）

同一对「朋友」可分属不同势力（小学朋友 vs 大学朋友）；同一个人可属多个势力（主角跨小学 / 中学 / 大学），布局上取主归属落块，次要归属用标记或放块交界。

---

## 4. 核心设计原则

### 4.1 中篇与长篇：两种体验，一套数据

| | 中篇（如《百年孤独》量级） | 长篇（如《红楼梦》量级） |
|--|---------------------------|--------------------------|
| 用户想要 | 一张读得懂的全景图 | 可导航的地图册，而非一张糊墙海报 |
| 默认展示 | 尽量展示主要人物与关系 | 按势力分块的大图 + 下钻 + 搜索 |
| 记忆与累计 | 较轻 | 按章入账；章间记忆属于后续辅助能力 |
| 防剧透 | 二期候选特色功能 | 二期候选特色功能，当前不实施 |
| 布局主轴 | 人少时可用亲疏 / 力导向 | 势力分区优先（见 §4.5） |

两者共享同一套按章积累的数据，区别仅在展示策略与默认过滤。

### 4.2 过程松、出口紧

| 交给系统灵活处理 | 由规则严格约束 |
|------------------|----------------|
| 怎么读章、是否搜索原文 | 输出结构（人物 / 关系 / 事件字段） |
| 推理与摘证据 | 关系类型优先归入预定义类型库；罕见但重要的允许登记为书内新类型 |
| 本章小结怎么写 | 写入前校验；本期按基础证据条件准入 |
| | 人物 ID / 关系方向由系统校验；展示用软硬权重与出现次数过滤 |

不把模型当「随便写库的上帝」，也不把模型当「只会填死表的表格工」。

### 4.3 记忆与账本分离

| 概念 | 用途 |
|------|------|
| **章总结 / 大总结** | 给系统的工作记忆（情节上下文） |
| **人名册** | 认人、管理别名 |
| **章结果数据** | 事实账本（出图的主数据源） |

出图以账本为准，不以总结复述为准。
### 4.4 百人级大图：势力分区是主策略

产品目标包含：**人物过百时，关系图仍然可用**——不是只能看 20 人子集，也不是只能聚焦后才看懂。

布局手段的优先级：

| 优先级 | 手段 | 作用 |
|--------|------|------|
| **P0 主策略** | **按势力分区**（§3.1） | 把 100+ 人拆成可读的块；块内局部布局，块间靠桥接人物 |
| P1 配套 | 边过滤、软边降权、块内折叠 | 块内过密时的二级收敛 |
| P1 配套 | 聚焦 / 搜索 | 导航，不替代全局分块秩序；章节切片另列二期候选 |
| P2 辅视图 | 亲疏扇区、族谱、力导向 | 可切换的辅模式，不能作为 100+ 全景的唯一依赖 |

**为何必须是势力分区：**

1. 按关系类型分区装不下规模——同学 / 朋友会跨多个团体，挤在同一区域必然混乱
2. 纯力导向在百人网络上不可读——缺乏语义分块，没有「从哪看起」
3. 小说人物爆炸时，团体归属往往比「和主角第几档亲」更稳——一章一世界、一校一圈、一府一党
4. 边表达关系、块表达归属——两层正交后，百人图才同时具备结构与细节

> **结论：** 100 人以上仍可用的大关系图，默认骨架 = 势力分区。过滤与聚焦是必要配套，亲疏中心图是有价值的辅模式。

无清晰势力可抽时，应用共现 / 阶段等启发式仍产出分区（可命名为「第 N 阶段」等），而非退回百人单层布局；实在无法分块时降级为强过滤子集 + 辅布局，并在界面标明。

---

## 5. 功能需求

### 5.1 输入与格式优先级

| 优先级 | 格式 | 说明 |
|--------|------|------|
| **P0** | **EPUB** | 主路径；章节切分、元数据清晰 |
| P1 | MOBI / AZW | 可转 EPUB 或二期原生支持 |
| P2 | TXT | 仅兼容纯文本稿场景 |
| 更后 | PDF | 排版与扫描噪声大 |

导入后内部统一为：书元数据 + 章节列表（标题 / 顺序 / 正文）。

### 5.2 主流程

```text
上传 EPUB
    ↓
解析为章节列表
    ↓
【可选·辅助】全书扫描人名候选 → 人名册草稿
    · 辅助对齐，非出图主路径
    · 长篇人物多时价值更大；中短篇可弱化
    ↓
【主路径】按章分析 + 按需消歧与补查
    · 输入：章节、人名册（可有可无）、前文摘要（长篇可选）
    · 系统按需读取章节正文、搜索取证
    · 产出：本章结构化数据（人物 / 关系 / 事件 / 证据）+ 本章摘要
    · 可回写：新别名进人名册
    ↓
【汇总出图】
    · 合并多章数据，去重归一
    · 应用软硬权重 + 出现次数等过滤
    · 当前汇总已完成分析的数据；防剧透切片后置
    ↓
交互图谱 + 导出
```

### 5.3 人名册（Cast）

**目标：** 尽量先对齐「书里有谁、都怎么叫」，再抽关系。

**定位：** 人名册是辅助工具；无完整全书扫描时，章分析仍可登记人物并出图。

最低字段：

| 字段 | 说明 |
|------|------|
| 正式名 / 稳定称呼 | 图上主显示名；无姓名人物用原文可稳定识别的称呼，不编造姓名 |
| 别名列表 | 有原文依据的外号、字号、稳定称谓；不包含仅在某次上下文中成立的泛称 |
| 简介 | 可选，如「贾府嫡孙」 |
| 性别 | 可选：男 / 女 / 未知；用于头像框配色 |
| 重要度 | 可选：主角 / 配角 / 龙套（可后算）；用于节点大小区分 |

规则：

- 首轮扫描允许粗糙，后续章分析可增补别名与新人物
- **无姓名但可稳定识别的具体人物也收录**，不以是否有专名作为准入条件；泛指群体、职业类别与无法区分具体人的称呼不因此自动成为人物
- **人物身份独立于名字**：关系关联具体人物；后续补充正式姓名或调整显示名，不改变人物及其关系的归属
- **别名归属主要在读章时完成**：结合正文和候选人物资料，判断新人或已有人物；新别名同时记录来源章节和归属理由，命中已有人物也不能漏掉新别名
- **名称只用于寻找候选，不直接证明同人**：允许同名或共享别名的不同人物，不因名称相同或首先匹配到某人就自动合并
- **稳定别名与本次指代分开**：“父亲”“师父”“那个老人”等称呼，先根据说话者和具体上下文确定本次指向；不能自动成为全书通用别名。只有原文支持稳定归属的称谓才作为人物别名展示
- **身份消歧由系统自动完成**：优先复用已有明确绑定；真正存在歧义时，结合相关人物资料及来源上下文做有限的针对性判断，不对所有名字重复审核
- 明确同一人的自动合并；信息不足时宁可暂时分开，也不要求用户逐项审核
- 本期不做人工合并 / 改名；身份归并由系统自动处理，不要求用户审核

### 5.4 章级分析

**触发：** 指定章节（或批量到第 N 章）。

**系统能力：**

| 能力 | 作用 |
|------|------|
| 获取章节正文 | 按章取文，可分段 |
| 章内搜索 | 按人名、关键词取证 |
| 查人名册 | 对齐正式名与别名 |
| 登记 / 更新人物 | 新人、新别名（受校验） |
| 提交本章结果 | 写入结构化数据（校验后） |

**本章产出：**

1. **章数据（账本）**
   - 本章涉及人物（尽量正式名 + 本章别名）
   - 关系记录（支持多条、多类型）
   - 事件（可选，便于解释）
   - 每条关系尽量带：章节号、说明、原文短句
2. **章摘要（记忆）**
   - 短段落，供后续章使用
   - 摘要过长时压缩为大摘要；后续上下文以大摘要 + 最近若干章细摘要为主

**记忆策略：**

```text
分析第 k 章的上下文 ≈
  大摘要（若有）
  + 最近若干章细摘要
  + 人名册摘要
```

### 5.5 关系模型：多标签 + 软硬权重

#### 原则

1. A 与 B 的关系是**标签集合**，不是单选
2. 一章写「师徒」、一章写「夫妻」——都可入账
3. **硬关系之间互不覆盖**（师徒与夫妻可并存），硬+中、中+中 同理
4. **已有硬或中关系时，不再抽取新的软关系**——不为同一对人物重复补充低信息量标签
5. **软关系是一种兜底**：优先找硬 / 中关系；两人之间尚无这类关系、但有实际交流依据时，才考虑记录软关系。典型场景是某人物与主角有交流，却没有更明确的关系；不据此限定为只能连接主角
6. **之前已经记录的软关系保留**：后来发现硬 / 中关系时不删除旧记录，默认图折叠到「更多」
7. 兜底不等于无依据地补边，也不把一次告别、敬仰表达等情节描述直接命名为一种新关系；这些内容可作为判断关系的上下文或事件
8. **未决硬 / 中关系先留到后续判断**，不在未决阶段抢先生成替代软关系。候选经后续判断被否定后，若该人物对没有其他成立的硬 / 中关系，再由 AI 根据交流与上下文寻找有依据的兜底关系；不能把被否定的标签机械降级成软标签
9. 后续判断仍未决时，继续内部保留，不当作已否定来触发兜底；AI 找不到有依据的兜底关系时允许不连边

#### 硬度分档（可配置）

| 档 | 示例类型 | 规则 |
|----|----------|------|
| **硬** | 夫妻、亲子、兄妹、师徒 | 并列展示；互不覆盖 |
| **中** | 主仆、上下级、同门、结盟 | 可并存；展示次于硬 |
| **软** | 朋友、相识、同场等粗粒度标签 | 有交流依据且该对无硬 / 中关系时作兜底；具体标签须与依据相符，不能从交流直接推成朋友 |

类型体系采用**半开放设计**：内置约 40～60 个高频类型，每项带软 / 中 / 硬属性；罕见但重要的关系允许登记为该书的新类型。

- 软类型宜粗（如「朋友」「相识」），不为每种交流行为另造标签；具体类型不得超出依据
- 高频亲属关系应尽量精确：父亲 / 母亲、哥哥 / 弟弟 / 姐姐 / 妹妹、祖父母 / 外祖父母、堂表亲等
- 粗粒度标签（如「亲子」「兄弟姐妹」）只在原文信息不足时作为兜底

#### 数据中的边

```text
A ↔ B（已有硬/中关系 → 不再新增软标签；旧软标签可保留在「更多」）
  标签:
    - 类型: 师徒, 硬度: 硬, 出现章节: [...], 证据: [...]
    - 类型: 夫妻, 硬度: 硬, 出现章节: [...], 证据: [...]

C ↔ D（仅有软关系 → 正常记录）
  标签:
    - 类型: 朋友, 硬度: 软, 出现章节: [...], 证据: [...]
```

汇总键：`(人物A, 人物B, 类型)` 合并章节列表与证据列表。有向关系保留双方角色与方向，不能将 A → B 和 B → A 自动合成同一条无向关系。

#### 展示强度（汇总时计算）

```text
分数 ≈ 类型基础分（硬 > 中 > 软）
      + 出现章数 × 系数
      + 有合格证据的加分
```

用于排序、默认过滤、弱化纯软边；**禁止**用分数让「夫妻」吞掉「师徒」。


### 5.6 证据（可解释）

每条关系记录尽量包含：

| 字段 | 说明 |
|------|------|
| 章节 | 出自哪一章 |
| 原文引用 | 原文短句（一两句） |
| 说明 | 一句话解释（本期基础模式必填） |

**本期准入：仅基础模式。**

- 有章节 + 非空说明；鼓励有原文引用，不以必须提供原文短句作为准入门槛
- 没有引文不自动否定关系；有引文时仍应校验其与正文一致，不能接受伪造摘录
- 加强模式（必须提供能在正文匹配的引文）后置，本期不提供模式切换

系统优先搜索到原文再摘录，减少凭空编造证据。

软关系也需要依据，但不要求原文直接出现「朋友」「相识」这样的标签；可依据实际交流及上下文判断。软 / 中 / 硬表示关系类型，不表示判断可信度。

**判断与补查规则：**

- 章分析完成首次语义判断；清晰关系通过基础证据与结构校验后即可准入，**本期取消每条关系都再次交给模型复核的全量门禁**
- **仅对明确未决的硬 / 中关系做有限补查**，围绕具体歧义提供首次判断理由、人物身份信息和相关原文；不能只给关系名称让系统从零翻书
- 软兜底依据实际互动及上下文生成，满足基础准入即可，不再进入通用关系复核；强候选否定后才找兜底的顺序仍按 §5.5 执行
- **取消通用收尾校对**：身份歧义在身份归属中处理；摘录不符在提交时反馈修正；关系语义疑点按本节补查，不积攒到全书最后统一修复
- 补查有明确上限、逐项保留结果，不以全部疑点解决作为交付其他合格结果的前提；超时、预算耗尽不等于关系被否定

**未决关系的处理：** 默认图只展示已满足准入条件的关系。自动补查后仍无法确认的记录在内部保留，不伪装成已确认关系，也不要求用户逐项审核才能出图。补查与判断应服务于自动产出可用图。

### 5.7 汇总与出图

**输入：** 当前已完成分析的数据集合。按阅读进度只使用前 N 章数据的历史切片功能后置，不属于当前核心范围。

**步骤：**

1. 人名以人名册对齐
2. 按（人物A, 人物B, 类型）合并
3. 计算展示分、应用软硬显示规则
4. 过滤过弱边 / 路人（规则可调，见 §5.7.7）
5. 生成图数据 → 前端展示

**防剧透（二期候选，当前不实施）：**

用户设定「读到第 N 章」→ 只使用前 N 章的数据出图。

**视图（长篇更重要）：**

- 默认核心人物子图
- 以某人为中心的关系网络
- 按关系类型筛选（只看血缘 / 只看硬关系）
- 按势力分区浏览（见 §3.1、§5.7.5）
- 展开「更多」查看被压制的软关系

#### 5.7.1 章节范围筛选（防剧透，二期候选）

以下为后续特色功能的目标行为，不纳入当前重构的核心验收。保留章节来源用于解释关系、按章分析和单章重跑，不代表已经提供防剧透保证。

| 交互 | 说明 |
|------|------|
| 章节滑块 / 输入框 | 拖动或输入 N，仅汇总前 N 章数据 |
| 快捷按钮 | 「全书」「前 1/4」「前半」等预设 |
| 实时反馈 | 调整后图上节点 / 边即时增减；被切掉的部分不渲染（不灰显，避免暗示剧透） |
| 章节标题预览 | 滑块旁显示当前 N 对应的章节标题，方便定位进度 |

#### 5.7.2 渲染样式：纯文字模式

适合快速浏览、以文字为主的场景：

```
  ┌──────────┐         师徒          ┌──────────┐
  │  唐僧     │ ──────────────────▶  │  孙悟空   │
  └──────────┘                       └──────────┘
                                        │
                                   夫妻  │
                                        ▼
                                    ┌──────────┐
                                    │  紫霞     │
                                    └──────────┘
```

| 要素 | 说明 |
|------|------|
| 节点 | 人物正式名或稳定称呼；可显示别名（小字） |
| 边 | 带箭头线条，线上标注关系类型 |
| 线条样式 | 硬关系实线、中关系虚线、软关系点线 |
| 箭头方向 | 无向关系双向或无箭头；有向关系单向箭头 |
| 布局 | 用户可选多种布局（见 §5.7.5）；节点可拖拽 |
| 悬停 | 高亮该节点所有邻边；显示关系详情浮窗（类型 + 章节 + 说明，有引文时展示原文） |

#### 5.7.3 渲染样式：头像框模式

在文字模式基础上叠加视觉区分，信息密度更高：

| 要素 | 说明 |
|------|------|
| 头像来源 | 优先用户上传；其次系统生成的占位头像（首期可用首字母色块） |
| 性别区分 | 头像框颜色区分：男蓝 / 女粉 / 未知灰（色板可配） |
| 重要度区分 | 主角头像更大 / 配角中等 / 龙套小号并半透明 |
| 边样式 | 同文字模式；关系标签悬浮在连线中点 |
| 组合展示 | 同一对人物的多条关系标签堆叠显示，可展开「+N」 |

两种模式可一键切换，共享同一份图数据与布局。

#### 5.7.4 人物聚焦模式

选中某个人物后，图收缩为以该人物为中心的关系网络。

**触发方式：**

- 点击节点 → 进入聚焦
- 搜索框输入人名 → 自动选中并聚焦
- 再次点击 / 点击空白处 / 点击「退出聚焦」→ 退出

**聚焦效果：**

| 要素 | 说明 |
|------|------|
| 中心节点 | 被选中人物放大居中、高亮 |
| 一度关系 | 直接关系的节点保留，正常渲染 |
| 无关节点 | 淡出隐藏（非删除，可恢复全局视图） |
| 边过滤 | 仅保留中心人物相关的边 |
| 关系标签 | 所有关系类型列在侧边栏，可逐条勾选筛选 |

**侧边栏信息面板（聚焦时）：**

```
┌─────────────────────────┐
│  ◎ 唐僧                  │
│  ─────────────────────  │
│  性别：男  重要度：主角    │
│  别名：玄奘、御弟         │
│  ─────────────────────  │
│  关系网络 (5)            │
│  ☑ 师徒 → 孙悟空   硬    │
│  ☑ 师徒 → 猪八戒   硬    │
│  ☑ 师徒 → 沙悟净   硬    │
│  ☐ 朋友 → 孙悟空   软    │
│  ☑ 敌对 → 白骨精   中    │
│  ─────────────────────  │
│  [展开二级] [退出聚焦]    │
└─────────────────────────┘
```

**联动规则：**

- 二期若实施章节筛选，与聚焦叠加生效——先切前 N 章，再聚焦某人
- 与渲染样式完全兼容——切换纯文字 / 头像框不影响聚焦状态

#### 5.7.5 布局模式与势力分区

> 势力定义见 §3.1。硬原则见 §4.5。

##### A. 两层正交信息

| 层 | 表达什么 | 视觉表现 |
|----|----------|----------|
| **势力（归属）** | 人属于哪个团体 | 平面分区 / 色块 / 外框 |
| **关系（连边）** | 两人之间是什么 | 线型、标签、线粗 |

禁止把「朋友」当成势力名；朋友是边，学校 / 教会才是块。

##### B. 布局模式

| 模式 | 定位 | 说明 |
|------|------|------|
| **势力分区** | 大图默认 / 主策略 | 团体块聚拢，块间弱连或经桥接人；百人级全景首选 |
| 亲疏扇区（主角中心） | 辅模式 | 适合已裁子集或聚焦后；不宜单独扛 100+ 全景 |
| 族谱 / 代际 | 辅模式（可后置） | 家族史、血缘为主的书或块内下钻 |
| 网络总览 | 兜底 / 小图 | 力导向等；小规模场景 |

用户可切换布局，但产品默认与「大图可用」验收以势力分区为准。

##### C. 势力从哪里来

| 来源 | 说明 |
|------|------|
| 机构 / 场景实体 | 文中明确的学校、教会、门派、府邸名 |
| 共现 / 生活世界 | 同一阶段大量共现、跨阶段很少重叠 → 聚成「第 k 阶段」再命名 |
| 家族作团体 | 贾府等作为一个势力块；房支 / 辈分是块内结构 |
| 人工校正（后续可选，本期不做） | 用户改势力名、合并 / 拆分块、改归属不属于本期能力，也不是分区的必要流程 |

一人多势力：允许。布局取主势力落块，其余用徽章 / 次要色标记，或放块交界。

##### D. 与过滤、聚焦的关系

- 势力分区解决「百人怎么摆」；过滤解决「块内是否过密、路人是否该上图」——两者叠加，不是二选一
- 目标若是 100+ 可浏览大图：应分区优先，过滤阈值可放宽；若只做 20 人子集，则未验证大图能力
- 聚焦 / 搜索用于块内下钻与找人；全局秩序仍靠势力块
- 块内过密时：二级折叠或块内再筛，不退回取消分区

#### 5.7.6 导出

| 格式 | 说明 |
|------|------|
| **PNG** | 导出当前画布为图片；包含分析范围、渲染样式、图例水印 |
| **SVG** | 矢量导出，方便二次编辑（首期可选） |
| JSON | 导出当前图数据（节点 + 边 + 证据），可重新导入；二期切片功能实施后遵循所选范围 |
| Mermaid | 生成 Mermaid 代码片段（后置） |

导出时：
- 自带图例（关系类型 → 线型 / 颜色对应表）
- 自带分析范围说明；二期防剧透实施后再附「前 N 章」阅读进度标注
- 头像模式导出时内嵌头像图片

#### 5.7.7 人物过滤（默认去路人）

长篇人物众多，不过滤会变成糊墙海报。默认过滤只出现极少数章节的路人，让图聚焦于有实际戏份的人物。

**出现次数的定义：**

采用**按章计数**：人物在某章的涉及人物列表中出现，计 1 次；同一章内提及多次仍计 1。

选择按章计数的理由：
- 与章级分析的产出天然对齐，无额外成本
- 计数来自本次汇总的数据；二期若实施章节切片，再按切片范围重算
- 稳定可复现

**过滤交互：**

| 交互 | 说明 |
|------|------|
| 滑块 | 「最少出现 N 章」可调，默认值 = 2 |
| 显示被过滤数 | 如「已隐藏 87 个路人角色」，可展开查看被过滤名单 |
| 例外保留 | 即使低于阈值，若人物有硬关系（如夫妻 / 亲子），仍保留 |
| 与章节切片联动（二期候选） | 切前 N 章后，出现次数自动按前 N 章重算 |
| 与聚焦联动 | 被聚焦人物无视阈值强制显示 |

阈值不写死：不同书人物密度差异大，固定值对短书太松、对长书可能太紧，因此提供滑块让用户自行微调。

### 5.8 自动完成与人工纠错边界

系统自动完成身份归并、关系归一、未决补查与异常处理，直接产出可用关系图。核心用户尚未读过书，没有能力承担事实校对；不能把人工纠错设计成正常流程、失败处理的必经步骤或质量兜底。

**本期不做：** 人工合并人物、改正式名 / 别名、增删改关系标签、修改势力与成员归属。此类能力留作后续可选方向，是否实施另行确定。

**本期保留：** 查看人名册、证据与分析进度，触发单章重跑，调整过滤阈值、聚焦、布局与导出。这些是查看与操作能力，不要求用户判定书中事实；重跑结果仍由系统自动处理。

---

## 6. 约束策略

| 项 | 做法 |
|----|------|
| 关系类型 | 预定义高频类型 + 书内半开放新类型；每项带软 / 中 / 硬属性 |
| 结构校验 | 人物 ID / 关系方向 / 类型引用由系统校验 |
| 关系归一 | 全书按语义去重；只对歧义部分做判断，不逐章重复 |
| 别名 / 同人 | 系统自动构建候选并处理歧义；包括可稳定识别的无姓名人物，本期不提供人工纠错 |
| 前文记忆 | 章摘要 + 大摘要压缩 |
| 证据 | 本期仅基础模式：章节 + 非空说明；引用可选，提供时校验 |
| 展示硬度 | 硬 / 中关系优先，软关系作兜底；已有软关系可被折叠但不删除 |
| 未决关系 | 后续自动判断；仍未决则内部保留，否定强关系后再由 AI 找有依据的软兜底；不阻塞其他合格结果出图 |

---

## 7. 与现有方案的差异

| 维度 | 重流水线方案 | ZhiYing |
|------|-------------|---------|
| 章内处理 | 整章塞入模板填大表 | 按需读取、搜索、写回 |
| 章间记忆 | 重结构化状态 | 摘要链 + 人名册 + 章数据 |
| 约束厚度 | 多层门控、长代码路径 | 薄约束，出口紧、过程松 |
| 关系模型 | 易做成单一类型演进 | 多标签 + 软硬展示 |
| 防剧透 | 能做但非主打 | 二期候选特色功能，当前不实施 |
| 出图 | 自研量化过滤 | 可后置借鉴，不阻塞最小可用版本 |

---

## 8. 范围划分与优先级

**Kotlin 重构首批交付范围：** 核心自动出图闭环包含团体分区和有向关系，两者均不后置到后续交付批次。具体开发故事可按依赖拆分实施。


**原则：** 质量和体验的关键在于**章内提取 + 按需消歧与补查 + 可读出图**。全书人名扫描与章间记忆属于辅助层——书越长、人越多，边际价值越大，因此放到二期。

防剧透也从当前核心范围移出，列为二期候选特色功能，是否实施另行确定。

**本期兼容边界：** 保留现有前端并适配本期功能；旧书籍都是测试数据，不要求迁移或兼容历史分析结果，可重新导入分析。现有评测集保留，与旧书运行数据区分；保留数据不代表认可旧评分机制或沿用旧分数。

### 8.1 一期（P0）：基础 + 中篇闭环 + 文字图谱

#### A. 基础能力

- [ ] EPUB 导入与章节切分
- [ ] 人物关系类型库（预定义类型 + 书内新增 + 校验）
- [ ] 模型接入（可配置，支持多厂商切换）
- [ ] 书籍管理与分析状态
- [ ] 分析进度展示

#### B. 章分析主路径（中篇优先）

- [ ] 章级分析：读章、搜索取证、提交结构化数据
- [ ] 自动身份消歧：候选生成 → 歧义判断 → 合并；用户无需逐项审核
- [ ] 关系归一：语义去重 → 类型归一 → 证据关联
- [ ] 展示策略：多标签数据保留，默认图只突出核心关系，弱关系折入「更多」
- [ ] 人名册：系统在分析中自动增补，支持查看，收录可稳定识别的无姓名人物
- [ ] 基础证据准入；未决强关系后续判断，被否定后再寻找有依据的兜底关系
- [ ] 单章 / 批量分析；章结果可缓存可重跑
- [ ] 汇总出图：合并已完成分析的数据，按关系强弱与人物出现频率过滤

> 一期可不做：全书人名扫描、章间记忆链 / 大摘要压缩、长篇波次调度。

#### C. 前端（一期就要能演示、好看）

- [ ] 纯文字节点图谱（可拖拽）
- [ ] 悬停查看关系详情（类型 + 章节 + 说明，有引文时展示原文）
- [ ] 人物聚焦（选中 → 一度关系网络 + 侧边栏筛选）
- [ ] 按出现频率过滤路人（阈值滑块）
- [ ] 导出：PNG + JSON
- [ ] 书籍上传、分析进度、人名册查看；保留现有前端并按本期范围适配
- [ ] 布局：小图可读即可（亲疏 / 力导向可先顶演示）
- [ ] 若一期要验收「100+ 人可用」→ 必须上势力分区（§4.5）；否则明确为「子集可读」

**一期明确后置：**

- 人工纠错：人物合并 / 改名、关系标签编辑、势力与成员修改
- 加强证据模式及证据模式切换
- 按阅读进度防剧透及前 N 章滑块（二期候选特色功能）
- 头像框渲染模式
- 全书人名扫描
- 章间记忆 / 长篇波次优化
- Mermaid / SVG 导出
- 族谱 / 代际布局

### 8.2 二期（P1）：长篇辅助 + 大图主策略落地

书越长、人物越多，下列价值越大：

- [ ] 候选特色功能：按阅读进度查看前 N 章关系图；是否实施另行确定
- [ ] 全书人名扫描：人名候选 + 频次 → 人名册草稿
- [ ] 章间记忆：摘要链 + 大摘要压缩；长篇波次并行时注入记忆快照
- [ ] 势力抽取 + 势力分区布局（主策略落地）
- [ ] 验收锚点：示例书（约 100+ 人物）在势力分区下可浏览
- [ ] 长篇默认视图与类型筛选
- [ ] 百人级自动身份消歧的准确率与结果质量
- [ ] 证据详情、多证据聚合与来源跳转
- [ ] 断点续跑、分析成本与进度展示
- [ ] 头像框模式（性别框色 / 重要度尺寸）

### 8.3 三期及更后（P2+）

- [ ] MOBI / TXT / PDF 支持
- [ ] 关系时间轴动画
- [ ] 自然语言改图
- [ ] 本地模型 / 私有化
- [ ] 中篇「一键全景」体验优化

---

## 9. 非功能需求

| 类 | 要求 |
|----|------|
| 成本 | 单章分析步数上限；可配置模型；缓存章结果避免重复消耗 |
| 可复现 | 章数据落盘；可重跑单章 |
| 隐私 | 用户书籍默认本地 / 私有；不用用户书做训练 |
| 合规 | 个人分析用途；不提供盗版书源；分享注意剧透与版权 |
| 性能 | 长篇允许数十分钟级整书分析；需进度展示与可中断 |

---

## 10. 成功标准

一期成功不看「一次全对」，而看：

1. 中篇跑通：上传 → 章分析 → 归纳 → 出图
2. 打开图默认能读（路人过滤生效），不是密网
3. 点开一条关系能看到来源章节与说明；有引用时可查看原文依据
4. 聚焦某人能看清其关系网；导出 PNG / JSON 可用
5. 未读过书的用户无需人工合并、校图或审核关系，也能直接得到可用结果

防剧透不属于一期成功标准；二期若决定实施，再单独验收。

**离线评测与分析诊断工作台：** 面向开发与评测维护者，沿用已确认原型，读取已有评测报告、标准标注与本地书籍分析产物，支持运行选择、指标浏览、问题筛选、逐章对照、人名册查看、刷新及 JSON 导出。历史评分仅按原报告展示，不代表当前产品质量达标；没有评测报告的分析只展示真实产物，不编造分数或判为通过。这是维护工具，不给核心读者增加人工审核流程。本轮不修改 Gold、评分机制或运行真实模型。

**量化质量目标待定。** 评测机制、指标口径与分数门槛由用户另行安排专项制定；当前保留评测集，不沿用简陋旧机制的综合分，也不把此前评论中的“90 分以上”作为已确认门禁。明确后的产品质量标准再写回本 PRD。

---

## 11. 开放问题

| 问题 | 备注 |
|------|------|
| 全书人名扫描用哪种方案 | 二期确定 |
| 软关系的具体标签与交流依据边界 | 兜底定位和旧记录保留规则已确定（§5.5）；同场等边界用具体样例进一步明确 |
| 首期默认模型选择 | 实现时确定；保持可切换 |
| 量化质量目标与评测口径 | 待专项制定；现有评测集保留，旧评分机制与分数不作为既定标准 |
| 联网百科补人名 | 覆盖窄，不做主路径；暂不排期 |

---

## 12. 术语表

| 词 | 含义 |
|----|------|
| 人名册 / Cast | 正式名或稳定称呼 + 别名的通讯录，包含可稳定识别的无姓名人物 |
| 账本 | 按章落盘的结构化人物 / 关系 / 事件数据 |
| 工作记忆 | 章摘要、大摘要——给系统的情节上下文 |
| 硬 / 中 / 软关系 | 类型展示优先级与是否可被压制 |
| 多标签 | 同一对人物可同时具备多种关系类型 |
| 防剧透切片 | 仅使用前 N 章数据汇总出图 |
| 势力 | 人物所属的团体聚合（见 §3.1） |

---

## 13. 文档修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-07-14 | 首版：汇总产品思路；EPUB 优先；多标签关系；人名册先行 |
| v0.2 | 2026-07-14 | 实现顺序：一期基础 + 中篇闭环 + 文字图谱；全书扫描与记忆放二期 |
| v0.5 | 2026-09-24 | 全文润色：剥离技术实现细节、精炼表达、理顺语句；产品决策不变 |
| v0.6 | 2026-09-24 | 确认软关系作兜底、已有硬 / 中关系时不再新增软关系、旧软关系保留并默认折叠；未决关系内部保留；防剧透后置到二期候选 |
| v0.7 | 2026-09-24 | 核心用户为未读过书的人；收录稳定可识别的无姓名人物；未决强关系后续否定后由 AI 找兜底；本期仅基础证据、不做人工纠错；保留前端与评测集、不要求旧测试书迁移；质量目标待专项制定 |
| v0.8 | 2026-09-24 | 别名在读章时按上下文归属；人物身份独立于名称，稳定别名与本次指代分开；取消全量关系复核及通用收尾校对，仅对明确未决硬 / 中关系有限补查，逐项保留结果 |
| v0.9 | 2026-09-24 | 团体分区及有向关系纳入 Kotlin 重构首批交付；失败与发布策略待确认 |

---

*本文档描述产品意图与边界，不绑定具体技术方案。实现时以本 PRD 为产品合同，细节可迭代。*
