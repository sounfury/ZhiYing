# 给人看的文档

这个文件夹写给人类维护者，不写给程序。

往这里加文档或改已有文档时，先让人能读懂：

- 用中文把事情说清楚，尽量别省略精简词语，比如关系归类->人物关系归类，不要省略人物。英文缩写、文件名、函数名可以出现（还是少出现一点好），但前面要先说明它是干什么的。
- 不要把 `few_long` 这种内部名字直接写进正文。要写读者能懂的行为，例如「各章一起读，互相看不见摘要」。
- 按事情发生的顺序写。
- 只写代码里已经在做的事。还没落地的方案放在 `docs/design/`。
