# language: en
@confirmed @prd_5_6
Feature: 基础证据准入与有限定向补查
  场景中的人物与类型引用有效且人物满足显示条件，除非场景另有说明。
  记录模型请求用于验证产品流程；不把模型自述作为请求完成的证据。

  @EVIDENCE_01
  Scenario: 没有引文也可满足基础准入
    Given 章分析明确支持一条关系
    And 关系包含有效来源章节和非空解释但未提供引文
    When 系统校验该关系
    Then 不因缺少引文拒绝准入
    And 该关系可纳入图谱并展示章节和解释

  @EVIDENCE_02
  Scenario Outline: 缺少必要证据字段不能准入
    Given 章分析明确支持一条关系
    And 关系“<缺失项>”
    When 系统校验该关系
    Then 返回对应字段错误
    And 该次无效提交不进入正式图谱
    Examples:
      | 缺失项             |
      | 没有来源章节       |
      | 解释为空字符串     |
      | 解释仅包含空白字符 |

  @EVIDENCE_03
  Scenario: 伪造摘录当次反馈修正
    Given 提交的关系引文在指定章节正文中不存在
    When 系统接收该关系
    Then 当次提交返回摘录不匹配错误
    And 不接受该伪造摘录
    And 不启动通用收尾校对

  @EVIDENCE_04
  Scenario: 引文合法不自动证明语义成立
    Given 师徒候选的引文确实存在于正文
    And 章分析认为称呼可能只是尊称并明确记录语义未决
    When 系统校验该引文
    Then 不仅因引文匹配就把师徒记为成立
    And 该候选进入有限定向补查而非默认图

  @REVIEW_01
  Scenario: 清晰关系不经过独立全量复核
    Given 章分析明确支持师徒关系并提供合格基础证据
    When 系统处理并汇总该关系
    Then 该关系可准入
    And 不为其发起独立关系复核请求
    And 不启动通用收尾 Agent

  @REVIEW_02
  Scenario: 合格软兜底不进入通用复核
    Given 有实际交流依据的相识兜底已满足基础准入
    When 系统处理该软关系
    Then 保留该合格关系
    And 不为其发起通用关系复核请求

  @REVIEW_03
  Scenario: 补查带上具体疑问和原文
    Given 师徒候选因“师父可能是尊称”而明确未决
    And 首次判断理由、人物身份、别名与原文来源已记录
    When 系统发起定向补查
    Then 补查输入包含该候选及具体疑问
    And 包含首次判断理由、相关人物身份信息和相关原文上下文
    And 不仅发送关系名称与章节摘要让模型从零翻书

  @REVIEW_04
  Scenario: 补查有上限且已完成判断不等待统一交卷
    Given 有两个明确未决的硬中关系候选
    And 测试运行的补查请求上限配置为 2 次
    And 第一个补查确认成立并已保存结果
    And 第二个补查仍不能确认且已达到请求上限
    When 系统结束补查并汇总
    Then 不再发起第三次补查请求
    And 第一个关系的合格结果可以出图
    And 第二个关系内部保留未决且不进入默认图
    And 不要求用户审核第二个关系
    And 不启动等待全部疑点解决后统一提交的收尾流程

  @REVIEW_05
  Scenario: 未决结果不混进更多关系
    Given 甲与乙已有一条合格关系和另一条未决候选
    When 用户展开该边的“更多”
    Then 未决候选不作为已确认关系展示
