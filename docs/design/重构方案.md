# ZhiYing 关系后处理 V2 重构方案

> 日期：2026-09-12  
> 状态：产品目标确认后的修订版
> 范围：人物身份归并、关系语义归一化、证据验证、默认展示收敛、异常修复、单章重跑一致性
> 核心原则：**程序负责准备有限问题，模型负责有限判断；账本尽量完整，默认关系图只展示最有价值的信息。**

---

## 0. 产品目标先于算法

本次重构不以“构建最严格的知识图谱”为目标，而以 ZhiYing 的实际用户目标为准：

> **用户把一本自己尚未读完、甚至基本没读过的书交给系统，系统应尽量自动生成一张完整、可信、重点清晰的人物关系图。**

因此，本次重构采用以下产品取舍。

### 0.1 完整性优先，但不能把不确定内容伪装成事实

目标不是“宁可漏掉也绝不犯错”，而是：

- 主要人物尽量不漏；
- 主要关系尽量不漏；
- 不确定信息继续自动处理，而不是立刻丢给用户；
- 最终仍无法确认的内容可以留在内部 `pending/unresolved`，但不能凭空变成 `confirmed`；
- 默认关系图不应被大量低价值或低置信度边淹没。

### 0.2 用户不是校对员

目标用户通常没有读过这本书，因此不能把以下任务当作正常产品流程：

- 让用户判断两个名字是不是同一个人；
- 让用户逐条处理 pending relation；
- 让用户选择几十个关系冲突；
- 让用户理解内部 predicate / relation registry；
- 每次分析后要求用户人工收尾才能得到可用图。

人工编辑继续保留，作为高级用户纠错能力，而不是系统正常完成分析的必要条件。

### 0.3 人物自动合并是核心能力，不是可选附加项

“同一个人物裂成多个节点”会直接破坏整张图，因此身份解析必须自动完成绝大多数工作。

原则：

- 先用确定性规则找候选；
- 对真正有歧义的 identity component 进行一次批量模型判断；
- 模型不能自由翻书调查；
- 信息不足时允许保留两个节点，但不要求普通用户介入；
- 错误合并仍然比暂时未合并更严重，因此所有 merge 必须经过服务端结构校验。

### 0.4 关系采用“半开放 registry”

不是完全自由生成，也不是死枚举。

采用：

```text
常见关系 seed registry
+ 全书级已有 learned registry
+ 必要时新增语义类型
```

即：

> **常见关系尽量直接归一；罕见但重要的文学关系允许新增。**

### 0.5 “事实保留”与“默认展示”分离

这是 V2 必须明确的产品原则。

例如：

```text
A 与 B：朋友 + 师徒
```

账本可以同时保留：

```text
friend_of
mentor_of
```

但默认图只需要显示：

```text
师徒
```

“朋友”进入详情里的“更多关系”，而不是被删除。

因此：

> **弱关系被强关系压制，是 Display Policy，不是事实冲突。**

这与 PRD 中“多标签 + 软硬展示”的原始设计保持一致。

### 0.6 不再默认运行“全书纠错总结 Agent”

旧设计把 Final Reconcile 当作每本书固定阶段。

V2 改为：

```text
正常情况：0 次 Final LLM

只有程序检测到少量、会明显污染最终图的高影响异常
→ ExceptionCase[]
→ 一次无工具批量判断
```

也就是说，**取消“先生成 suspects，再让 Agent 自己调查整本书”的产品概念。**

---

## 1. 当前问题的真正根因

目前性能问题不是《悉达多》太长，也不是几十条关系天然需要大量推理，而是旧架构把一个有限数据整理问题变成了多轮 Agent 调查。

### 1.1 pending 被错误提升成全书疑点

当前：

```python
detect_missing_evidence()
```

会把每一条：

```text
status == "pending"
```

直接变成一个 `MissingEvidence suspect`。

因此：

```text
22 suspects
```

很可能只代表：

```text
22 条还没被证据验证器确认的 occurrence
```

并不代表 22 个全书级错误。

V2 中：

```text
pending relation
→ 普通待判定状态
≠ relation conflict
≠ Final Reconcile case
```

### 1.2 相同语义按章节重复处理

当前虽然已经有 batch 和任务级 cache，但主要入口仍然是：

```text
for chapter:
    normalize_relations(chapter.relations)
```

这会让章节边界影响关系归一化。

V2 的单位改为：

```text
全书唯一 RelationSemanticCluster
```

### 1.3 Final Reconcile 是自由探索式 Tool Agent

现有模式：

```text
全书 prompt
→ LLM
→ 查工具
→ LLM
→ 再查工具
→ ...
→ submit_reconciliation
```

即使裁剪历史，它仍然具有以下问题：

- 调用次数不可自然预测；
- 模型可以不断扩大调查范围；
- 重复搬运相同 cast / ledger / chapter 信息；
- pending 越多，探索动力越强；
- 最终收益与成本不成比例。

这不是需要继续调 Prompt 的问题，而是执行模型不适合当前任务。

---

## 2. V2 总体架构

新的主路径：

```text
Chapter Agent 原始抽取
  ↓
CastWriter / rebuild_cast
  ↓
Identity Candidate Compiler
  ↓
Identity Resolver（仅歧义 component，0～N 有限批次）
  ↓
确定性应用人物 merge / alias
  ↓
Relation Preprocessor
  ↓
全书 Relation Cluster Compiler
  ↓
Semantic Resolver（只处理歧义 cluster）
  ↓
Evidence Resolver（局部窗口、全书收集、有限 batch）
  ↓
Canonical Fact Compiler
  ↓
Display Policy Compiler
  ↓
High-impact Anomaly Compiler
  ↓
是否存在真正需要语义修复的 ExceptionCase？
  ├─ 否 → 完成
  └─ 是 → Exception Resolver（无 tools，一次/有限 batch）
             ↓
           Validator
             ↓
           Patch Compiler / Applier
  ↓
事务性发布
```

与旧架构最大的区别：

> **不再存在一个“最后再读一遍全书，看看哪里不对”的模型。**

---

## 3. Identity Pipeline：自动认人优先

身份错误对关系图的破坏大于单条关系错误，因此 Identity Pipeline 应放在关系后处理之前。

### 3.1 Identity Candidate Compiler —— 确定性

输入：

- cast；
- 各章 `ChapterPerson`；
- canonical name；
- aliases；
- 来源章节；
- 必要的局部出现文本。

构建 identity candidate graph。

边来源包括：

- A canonical name == B alias；
- alias overlap；
- 标准化后名称一致；
- 同一章节/相邻章节中稳定称谓切换；
- 已有人工作为强覆盖。

但这里只生成候选，不直接因为字符串相似就合并。

### 3.2 用连通分量替代人物两两 suspect

例如：

```text
A ↔ B
B ↔ C
A ↔ C
```

不生成 3 个问题。

改为：

```text
IdentityComponent {A, B, C}
```

一个 component 一次判断。

### 3.3 Identity Resolver —— 批量判断，不探索

建议数据结构：

```python
class IdentityCase:
    case_id: str
    person_ids: list[str]
    canonical_names: list[str]
    aliases: dict[str, list[str]]
    chapter_ids: list[int]
    evidence_snippets: list[str]
```

模型输出必须明确分组：

```python
class IdentityDecision:
    case_id: str
    groups: list[list[str]]
    canonical_keep_id: dict[str, str]
    alias_additions: dict[str, list[str]]
    unresolved_person_ids: list[str]
    reason: str
```

模型只能引用输入中的 person_id。

### 3.4 自动化策略

- 明确同人 → 自动 merge；
- 明确不同人 → 保持分离；
- 信息不足 → 保持分离，不要求普通用户处理；
- manual merge / rename 永远优先；
- merge 前必须验证不存在未知 ID、自环、环状 merge 和非法跨对象修改。

V2 不再把“待合并队列”作为普通用户必须完成的步骤。

---

## 4. Relation Preprocessor —— 完全确定性

输入：所有已完成章节的原始关系 occurrence。

职责：

1. quote 精确定位；
2. 标准化空白、全半角和无语义标点差异；
3. 无向关系人物对排序；
4. 校验 person_id；
5. 保留 `raw_relation`；
6. 生成稳定 occurrence / evidence hash；
7. 计算完整 descriptor fingerprint。

### 4.1 exact fingerprint 必须保守

安全 exact fingerprint 使用标准化后的完整 descriptor：

```python
RelationFingerprint = {
    "label": str,
    "category": str,
    "definition": str,
    "directed": bool,
    "subject_role": str,
    "object_role": str,
}
```

不能为了减少 cluster 数，在确定性阶段擅自通过模糊字符串相似度合并语义。

---

## 5. Relation Cluster Compiler —— 全书级处理

目标：

> 模型处理唯一语义簇，不处理每一条 occurrence。

建议结构：

```python
class RelationSemanticCluster:
    cluster_id: str
    member_relation_ids: list[str]
    representative: RelationDescriptor
    observed_labels: list[str]
    observed_raw_relations: list[str]
    chapter_ids: list[int]
    candidate_predicates: list[str]
```

流程：

```text
所有 occurrences
→ exact descriptor dedupe
→ registry alias / exact match
→ compatible candidate retrieval
→ unresolved semantic clusters
```

相同关系在 12 章出现 20 次：

```text
20 occurrences
→ 1 semantic cluster
→ 最多一次 semantic decision
→ 广播给 20 条 occurrence
```

---

## 6. Semantic Resolver —— 半开放关系类型

### 6.1 模型不读取完整 registry

每个 cluster 只携带：

- cluster descriptor；
- 代表性 raw descriptions；
- 有限 candidate predicates；
- cluster_id。

候选检索顺序：

1. 完整 descriptor exact；
2. alias exact；
3. label exact；
4. `category + directed + roles` 兼容过滤；
5. 必要时 lexical shortlist。

### 6.2 `new` 必须谨慎

如果 shortlist 召回漏掉已有等价 predicate，就会把 registry 再次拆碎。

因此：

- `existing` 只能引用提供的 predicate；
- `new` 只在服务端确认已检查兼容候选集合后允许；
- 模型不生成 predicate 主键；
- `new` 由服务端 `register()` 分配稳定 ID；
- 候选召回不足时应 `unresolved`，不能为了完成率强造新类型。

建议输出：

```python
class SemanticDecision:
    cluster_id: str
    action: Literal["existing", "new", "unresolved"]
    predicate: str | None
    reverse: bool
    reason: str
```

### 6.3 批处理

按 cluster 数和输入字符/token 双限制切 batch，不按章节切。

短篇正常目标：

```text
0～1 次 semantic request
```

硬上限可以按规模动态计算，而不是按章线性增长。

---

## 7. 初始关系类型：从 13 类扩展为“小而够用”的 seed registry

当前 seed registry 只有 13 类，且所有 `display_priority` 基本等价，不足以支持“强关系压制弱关系”的产品目标。

V2 建议给 `RelationDefinition` 增加：

```python
display_tier: Literal["hard", "mid", "soft"]
display_priority: int
```

必要时以后再增加：

```python
structural_constraints: ...
```

但第一版不要建立重量级 ontology。

### 7.1 建议的 seed 集合

以下是种子，不是合法值全集。

#### Hard —— 通常应成为默认图核心标签

亲属关系是人物图里最需要精确表达的一类。对中文小说尤其不能把“哥哥 / 弟弟 / 表兄 / 堂妹”全部压成“兄弟姐妹 / 堂表亲”。

因此 Hard seed 分成两层：

```text
精确常用亲属类型 —— 有明确原文时优先使用
宽泛 fallback 类型 —— 原文本身不够具体时才使用
```

##### A. 精确常用亲属类型

| predicate | label | 说明 |
|---|---|---|
| `husband_of` | 丈夫 | A 是 B 的丈夫 |
| `wife_of` | 妻子 | A 是 B 的妻子 |
| `father_of` | 父亲 | A 是 B 的生父 |
| `mother_of` | 母亲 | A 是 B 的生母 |
| `adoptive_father_of` | 养父 | A 是 B 的养父 |
| `adoptive_mother_of` | 养母 | A 是 B 的养母 |
| `older_brother_of` | 哥哥 | A 是 B 的年长男性手足 |
| `younger_brother_of` | 弟弟 | A 是 B 的年幼男性手足 |
| `older_sister_of` | 姐姐 | A 是 B 的年长女性手足 |
| `younger_sister_of` | 妹妹 | A 是 B 的年幼女性手足 |
| `paternal_grandfather_of` | 祖父 | A 是 B 的父系祖父 |
| `paternal_grandmother_of` | 祖母 | A 是 B 的父系祖母 |
| `maternal_grandfather_of` | 外祖父 | A 是 B 的母系祖父 |
| `maternal_grandmother_of` | 外祖母 | A 是 B 的母系祖母 |
| `elder_paternal_uncle_of` | 伯父 | A 是 B 父亲的哥哥 |
| `younger_paternal_uncle_of` | 叔父 | A 是 B 父亲的弟弟 |
| `paternal_aunt_of` | 姑姑 | A 是 B 父亲的姐妹 |
| `maternal_uncle_of` | 舅舅 | A 是 B 母亲的兄弟 |
| `maternal_aunt_of` | 姨妈 | A 是 B 母亲的姐妹 |
| `paternal_older_male_cousin_of` | 堂兄 | A 是 B 年长的男性堂亲 |
| `paternal_younger_male_cousin_of` | 堂弟 | A 是 B 年幼的男性堂亲 |
| `paternal_older_female_cousin_of` | 堂姐 | A 是 B 年长的女性堂亲 |
| `paternal_younger_female_cousin_of` | 堂妹 | A 是 B 年幼的女性堂亲 |
| `older_male_cousin_of` | 表兄 | A 是 B 年长的男性表亲 |
| `younger_male_cousin_of` | 表弟 | A 是 B 年幼的男性表亲 |
| `older_female_cousin_of` | 表姐 | A 是 B 年长的女性表亲 |
| `younger_female_cousin_of` | 表妹 | A 是 B 年幼的女性表亲 |
| `lover_of` | 恋人 | 明确双向恋爱/情侣关系 |
| `mentor_of` | 师徒 | 明确师承 |

这里不要求把中文亲属称谓词典穷举完，但**高频且能显著帮助读者理解人物关系的称谓应直接进入 seed**。

##### B. 宽泛 fallback 亲属类型

| predicate | label | 使用条件 |
|---|---|---|
| `spouse_of` | 夫妻 | 明确是配偶，但无法或无需区分丈夫/妻子 |
| `parent_of` | 亲子 | 明确父母子女，但性别/角色信息不足 |
| `adoptive_parent_of` | 养亲 | 明确养育亲子，但信息不足以细分养父/养母 |
| `sibling_of` | 兄弟姐妹 | 明确手足，但无法确定年龄/性别角色 |
| `grandparent_of` | 祖孙 | 明确祖孙，但父系/母系或性别不明确 |
| `uncle_aunt_of` | 叔侄/舅甥等 | 明确隔一代近亲，但具体支系不明确 |
| `cousin_of` | 堂表亲 | 明确同辈堂表亲，但无法进一步判断堂/表、年龄或性别 |

**禁止把已有精确信息降级成 fallback。** 例如：

```text
原文明示“哥哥” → older_brother_of
不得为了复用 registry → sibling_of

原文明示“表弟” → younger_male_cousin_of
不得归一成 cousin_of
```

Semantic Resolver 的候选排序必须遵守：

```text
精确等价 > 精确常用 seed > 宽泛 fallback
```

只有原文证据本身不够具体时，才允许使用宽泛类型。

#### Mid —— 有稳定剧情意义，但通常次于亲属/配偶/正式师承

| predicate | label | 说明 |
|---|---|---|
| `romantic_interest_in` | 单恋 | 有向，不等价于恋人 |
| `master_of` | 主仆 | 主人 → 仆从 |
| `superior_of` | 上下级 | 组织上下级 |
| `colleague_of` | 同事 | 稳定共事 |
| `business_partner_of` | 商业伙伴 | 商业合作关系 |
| `classmate_of` | 同学 | 共同就学 |
| `fellow_disciple_of` | 同门 | 同一师承/门派中的同门 |
| `ally_of` | 盟友 | 稳定联盟 |
| `enemy_of` | 敌对 | 明确敌对 |
| `rival_of` | 对手 | 竞争/对手，不必达到敌对 |
| `protector_of` | 保护 | A 对 B 有稳定保护职责/行为 |
| `benefactor_of` | 恩人 | A 对 B 有重要帮助或恩惠 |
| `companion_of` | 同行/搭档 | 稳定同行或搭档关系 |

#### Soft —— 账本可保留，默认易被压制

| predicate | label | 说明 |
|---|---|---|
| `friend_of` | 朋友 | 明确友谊 |
| `acquaintance_of` | 相识 | 认识但无更强类型 |
| `interacts_with` | 互动 | 只作为最低层 fallback，默认图可隐藏 |

罕见但重要的关系，例如：

```text
债主/债务人
医生/病人
审判者/被审判者
绑架者/被绑架者
宗教引路人
政治婚约
```

不必提前全部塞进 enum；可以由半开放 registry 学习。

### 7.2 seed 数量原则

目标不是“类型越多越好”，但**常用亲属称谓属于读者真正关心的信息，不能为了控制枚举数量而过度压缩。**

加入精确中文亲属关系后，推荐初始规模约：

```text
40～60 个常见类型
```

其中很大一部分是稳定、低歧义的 Hard kinship seeds；社交、事件类仍保持克制。

原则：

- 父亲 / 母亲、哥哥 / 弟弟 / 姐姐 / 妹妹、祖父母 / 外祖父母、伯叔姑舅姨、堂兄弟姐妹、表兄弟姐妹等高频称谓直接内置；
- `亲子 / 兄弟姐妹 / 堂表亲` 等宽泛类型只做 fallback；
- 罕见称谓不要求首版穷举，遇到后仍可由半开放 registry 学习；
- seed 扩充的目的是减少模型归一化成本并提升图的可读性，不是建立完整中文亲属本体。

### 7.3 精确关系与反向展示

关系事实应有唯一 canonical 方向，避免同一个事实因为“父亲 / 儿子”“哥哥 / 弟弟”被保存成两份。

例如：

```text
甲是乙的哥哥
乙是甲的弟弟
```

应归并为同一个 canonical fact，而不是两条独立事实。

第一版可以让 canonical predicate 表示其中一个稳定方向，例如：

```text
older_brother_of(甲, 乙)
```

前端或 Display Policy 根据方向展示对应的人类可读称谓。后续如有需要，可给 RelationDefinition 增加：

```python
forward_label: str
reverse_label: str | None
```

或等价的 role-based display 机制。

因此，**seed 精细化不等于把互为逆关系的数据存两遍。**

---

## 8. Evidence Resolver —— 尽量自动消化 pending

用户选择的是：

> pending 是临时状态，系统应尽量自动处理，而不是把它变成人工任务。

因此证据处理分成三层。

### 8.1 Locator：确定性定位

结果不是简单 true/false，而建议明确：

```python
locator_status: Literal[
    "located",
    "missing",
    "ambiguous",
]
```

注意：

```text
quote 没找到 / 出现多次
```

代表：

```text
无法验证
```

不代表：

```text
关系被原文否定
```

因此 locator 失败应保持 `pending`，不能直接设为 `rejected`。

### 8.2 Evidence Case 全书收集

```python
class EvidenceCase:
    case_id: str
    relation_id: str
    person_a: str
    person_b: str
    predicate: str | None
    raw_relation: str
    quote: str
    local_context: str
```

模型只判断：

```text
confirmed / rejected / pending
```

### 8.3 context window 去重

不能为了“1 次请求”把几十个 1600 字窗口机械拼成一个巨型 prompt。

先按：

```text
(chapter_id, overlapping context range)
```

合并窗口，让同一段正文中的多个关系共享 context。

再按 token/字符预算切有限 batch。

因此验收目标改为：

> **Evidence 请求数低且有上限，token 不重复搬运；不强求任何书都必须恰好 1 次请求。**

### 8.4 二次处理只针对高影响 pending

Evidence Resolver 一次批量判断后仍 pending 的关系：

- soft relation：保留内部 pending，默认不展示；
- mid relation：按展示影响决定是否进入 ExceptionCase；
- hard relation：若会明显改变默认图，可进入 High-impact Exception Resolver；
- 不允许所有 pending 再次进入“总校对”。

---

## 9. Canonical Fact Compiler —— occurrence 与图上事实分离

多个章节里的同一关系不是多条图边，而是一个 canonical fact 的多份 evidence occurrence。

建议：

```python
CanonicalRelationFact:
    fact_id: str
    person_a: str
    person_b: str
    predicate: str
    relation_ids: list[str]
    chapter_ids: list[int]
    evidences: list[EvidenceRef]
    confirmed_count: int
    pending_count: int
```

无向 relation person pair 强制排序。

账本 occurrence 继续保存，Canonical Fact 用于汇总和展示。

---

## 10. Display Policy Compiler —— 解决“关系太多”，而不是让 Reconcile 删事实

这是 V2 新增的正式层。

### 10.1 默认展示规则

同一人物对允许有多个真实 relation fact，但默认标签只显示最核心的少数几个。

建议第一版：

```text
若存在 hard：
    显示全部 hard（通常数量很少）
    mid 最多再显示 1 个高价值且语义不同的
    soft 全部折叠到“更多”

若无 hard 但存在 mid：
    默认显示 1～2 个 mid
    soft 折叠

只有 soft：
    默认显示最高优先级 1 个
```

这是 UI/display 策略，不修改 ledger。

### 10.2 例子

```text
悉达多 ↔ 乔文达

账本：
- 朋友 [soft]
- 同行 [mid]
- 精神追随 / 同门（若成立）[mid]

默认图：
- 显示最高价值的 1～2 个
- “朋友”可在详情中展开
```

```text
A ↔ B

账本：
- 夫妻 [hard]
- 朋友 [soft]
- 同事 [mid]

默认图：
- 夫妻
- 必要时同事
- 朋友折叠
```

### 10.3 不再把“多标签”视作冲突

以下情况正常：

```text
夫妻 + 同事
朋友 + 师徒
盟友 + 同门
```

V2 不创建 `type_clash` 之类的通用冲突。

---

## 11. 取消固定 Final Reconcile Agent

这是本次修订相对原方案最大的变化。

### 11.1 删除的概念

不再采用：

```text
每本书
→ SuspectsGenerator
→ Final Reconcile Agent
→ Agent 自己搜索 / 读章 / 查询 cast
→ 多轮 submit
```

也不再要求：

```text
Final Reconcile 正常必须调用 1 次
```

新目标是：

```text
正常书：0 次 Final LLM
```

### 11.2 High-impact Anomaly Compiler

程序只寻找会明显污染最终关系图的异常。

第一版范围应非常保守，例如：

1. unresolved identity component 涉及高重要度人物；
2. hard/mid semantic cluster 无法归一，但会进入默认图；
3. hard relation evidence 在正常 Evidence Resolver 后仍有关键歧义；
4. 明确违反数据结构不变量的关系；
5. registry 中出现真正的 duplicate semantic definition 候选；
6. 只有在 relation type 明确定义了不可共存的结构约束时，才生成 structural conflict。

普通 pending 不属于 anomaly。

### 11.3 不自动猜“关系冲突”

以下均不能仅凭结构判定为冲突：

```text
A 爱慕 B，同时 B 爱慕 A
A 支持 B，同时 B 支持 A
同一对人物有朋友 + 师徒
同一关系在多个章节重复出现
某 occurrence rejected，另一个 occurrence confirmed
```

小说关系还会随时间变化，因此不能简单把跨章状态差异解释为矛盾。

### 11.4 Exception Resolver —— 无工具、有限输入

若确实存在 case：

```python
class ExceptionCase:
    case_id: str
    case_type: str
    related_ids: list[str]
    local_evidence: list[str]
    deterministic_facts: dict
    allowed_actions: list[str]
```

一次提供完整有限材料。

禁止：

- 搜索整本书；
- 调用工具；
- 自己扩大上下文；
- 自己创建 ID；
- 第二轮自由调查。

信息不足返回：

```text
unresolved
```

即可。

---

## 12. Decision 必须强类型，不能使用模糊 target_ids

原方案中统一的：

```python
ReconcileDecision(action, target_ids)
```

表达力不足。

V2 使用 discriminated union。

例如：

```python
class MergePersonsDecision:
    case_id: str
    action: Literal["merge_persons"]
    keep_id: str
    drop_ids: list[str]
    reason: str

class AddAliasDecision:
    case_id: str
    action: Literal["add_alias"]
    person_id: str
    aliases: list[str]
    reason: str

class RemoveRelationDecision:
    case_id: str
    action: Literal["remove_relation"]
    relation_ids: list[str]
    reason: str

class ReplacePredicateDecision:
    case_id: str
    action: Literal["replace_predicate"]
    relation_ids: list[str]
    predicate: str
    reverse: bool
    reason: str

class UnresolvedDecision:
    case_id: str
    action: Literal["unresolved"]
    reason: str
```

`PatchCompiler` 只做机械转换，不进行新的语义推断。

---

## 13. Validator 必须从旧 submit 工具中抽出来复用

当前 `submit_reconciliation` 已经实现了一部分有价值的校验：

- person ID 存在；
- merge keep/drop 不同；
- merge cycle；
- duplicate drop；
- remove relation_id 必须存在；
- relation endpoint 必须合法；
- evidence chapter 必须存在。

不要重新写一套平行逻辑。

将其抽成：

```text
ReconcileValidator / DecisionValidator
```

并增加：

- case_id 必须来自当前 batch；
- action 必须属于 case.allowed_actions；
- predicate 必须存在于 registry；
- 模型不得改写 quote；
- 模型不得引入未知 relation/person；
- merge 后不得生成 self-loop；
- 人工 edit 不得被自动 patch 覆盖。

---

## 14. 单章重跑：V2 先选择正确性，不做复杂增量失效

现有单章重跑只调用：

```text
enrich_ledger(rerun chapter)
→ whole-book reconcile
```

V2 不能继续这样做，因为新的 semantic cluster、identity decision 和 display facts 都是全书级产物。

### 14.1 V2 明确策略

单章重跑成功后：

```text
1. staging 中替换该章 raw extraction snapshot
2. 从所有有效 raw extraction snapshots 重建 cast
3. replay manual edits
4. 重新运行全书 Identity Pipeline
5. 重新运行全书 Relation Pipeline
6. 重新构建 canonical facts / display policy
7. 仅有 high-impact exception 时运行 Exception Resolver
8. 重新生成依赖结果
9. transaction publish
```

不是重新抽取整本书，只是重新跑成本已经被压低的全书后处理。

### 14.2 为什么暂不做增量

如果 V2 后处理已经只需要个位数甚至 0～几次模型调用，那么：

```text
复杂 dependency invalidation
```

带来的工程风险大于节省的成本。

以后若《白痴》等长书证明全书后处理仍昂贵，再增加增量 cluster invalidation。

---

## 15. 新的完整数据流

```text
1. Chapter Agent
   - 原始人物候选
   - 原始关系 occurrence
   - 原始 evidence
   ↓
2. 保存 raw extraction snapshot
   ↓
3. Cast rebuild / deterministic merge
   ↓
4. Identity Candidate Compiler
   ↓
5. Identity Resolver（仅歧义 component）
   ↓
6. Identity Patch Validator + deterministic apply
   ↓
7. Relation Preprocessor
   ↓
8. Relation Cluster Compiler（全书）
   ↓
9. Semantic Resolver（歧义 cluster）
   ↓
10. Evidence Resolver（全书 case + 去重 context window）
   ↓
11. Canonical Fact Compiler
   ↓
12. Display Policy Compiler
   ↓
13. High-impact Anomaly Compiler
   ↓
14. cases == 0 ?
   ├─ yes → 完成
   └─ no  → Exception Resolver（无 tools）
             ↓
           Validator / Patch Compiler / Applier
   ↓
15. 保存 pre-publish staging
   ↓
16. 发布
```

---

## 16. 模块调整建议

### 保留并强化

```text
backend/app/core/relation_registry.py
backend/app/core/evidence.py
backend/app/core/patch_applier.py
backend/app/agent/llm.py
backend/app/storage/filestore.py
backend/app/core/rebuild.py
```

### 重写 / 重定位

```text
backend/app/agent/relation_normalizer.py
backend/app/agent/relation_pipeline.py
backend/app/agent/relation_verifier.py
backend/app/core/suspects.py
backend/app/core/reconcile_service.py
backend/app/agent/reconcile_agent.py
```

### 新增

```text
backend/app/core/identity_cases.py
backend/app/agent/identity_resolver.py
backend/app/core/identity_validator.py

backend/app/core/relation_preprocessor.py
backend/app/core/relation_clusters.py
backend/app/agent/semantic_resolver.py
backend/app/core/evidence_cases.py
backend/app/agent/evidence_resolver.py
backend/app/core/canonical_facts.py
backend/app/core/display_policy.py

backend/app/core/anomaly_compiler.py
backend/app/agent/exception_resolver.py
backend/app/core/decision_validator.py
backend/app/core/reconcile_patch_compiler.py
```

### 最终删除

完成迁移后删除 Final Reconcile tool loop 相关：

```text
make_reconcile_tools()
ReconcileToolContext
run_reconcile_agent() 的 tool loop
MAX_RECONCILE_STEPS
RECONCILE_HISTORY_MESSAGES
RECONCILE_TOOL_RESULT_CHARS
```

`PatchApplier` 不需要因为 Agent 被删除而一起删除。

---

## 17. 调用次数与成本目标

### 17.1 短篇《悉达多》

不含 Chapter Agent 正文抽取。

| 阶段 | 正常目标 | 说明 |
|---|---:|---|
| Identity resolve | 0～1 | 仅 identity component 有歧义时 |
| Semantic normalize | 0～1 | 全书 semantic cluster 批量 |
| Evidence verify | 1～2 | 依 token/context window 切 batch，不强求 1 次 |
| Exception resolve | 0 | 只有高影响异常时才调用 |
| **后处理总计** | **1～4 次** | 正常不应存在 Final Agent loop |

Exception Resolver 即使触发，也必须是有限 batch，不得进入自由多轮。

### 17.2 Token 目标

《悉达多》后处理建议：

```text
正常 input tokens < 100k
架构报警线 > 150k
性能回归线 > 200k
```

重点不是追求请求数绝对最小，而是：

- 不重复携带全文；
- 不重复携带完整 registry；
- 相同语义只决策一次；
- 相同 context window 尽量复用；
- 不存在累积 Agent history。

### 17.3 长书扩展规律

模型成本主要随以下量增长：

```text
ambiguous identity components
+ ambiguous semantic clusters
+ unique evidence windows/cases
+ high-impact exception cases
```

而不是简单随：

```text
chapter_count
raw relation occurrence count
suspect count
```

线性增长。

---

## 18. 正确性验收：不能只看 precision

产品目标是“尽量完整”，因此不能通过把大量关系留 pending 来获得漂亮 precision。

### 18.1 当前《悉达多》Gold 基线

当前 evaluator 报告：

```text
总分：85.01 / 100
人物 Entity F1：94.12%
同人合并准确率：87.50%
Required 关系召回：79.17%
Optional 覆盖：86.67%
Forbidden 避免率：87.50%
证据原文精确命中：83.61%
关系去重质量：83.61%
章节输出覆盖：91.67%
```

### 18.2 V2 必须同时观察

- Entity F1；
- identity split / wrong merge；
- Required relation recall；
- Optional coverage；
- Forbidden avoidance；
- evidence exactness；
- semantic duplicate rate；
- 默认图 relation precision；
- 默认图平均每对人物显示标签数；
- hidden soft relation 数；
- unresolved hard/mid 数；
- 模型请求数；
- input/output tokens；
- wall time。

### 18.3 两套 evaluator

必须拆开：

#### A. Postprocess evaluator

固定 raw extraction snapshots。

只比较：

```text
旧后处理
vs
V2 后处理
```

这样才能知道重构本身是否改善 identity、归一化、证据、去重和成本。

#### B. End-to-End evaluator

从 Chapter Agent 开始重新分析整本书。

用于测最终产品质量。

不能把 Chapter Agent 随机波动误判成后处理架构回归。

---

## 19. 新增测试

### 19.1 Identity

1. A/B/C 别名图形成一个 component，而不是 3 个 suspect；
2. component 一次判断返回两组人物；
3. 不同人共享泛称不得误 merge；
4. manual merge/rename 优先；
5. merge 后关系 endpoint、event、override 全部正确 remap；
6. wrong merge fixture 必须为 0。

### 19.2 Semantic clustering

1. 20 条相同 descriptor → 1 cluster；
2. 跨 12 章相同 descriptor → 1 cluster；
3. 同 label 不同 definition 不得 exact 合并；
4. directed/undirected 不得合并；
5. role 不等价不得合并；
6. candidate retrieval 必须包含真正已有 predicate 的测试。

### 19.3 Evidence

1. quote missing → pending，不是 rejected；
2. quote ambiguous → pending；
3. 同一 context window 中多个 relation 共用正文片段；
4. provider failure → 保留 pending；
5. soft pending 不进入 ExceptionCase；
6. hard pending 可按影响进入 ExceptionCase。

### 19.4 Display Policy

1. `朋友 + 师徒` → 账本两条都在，默认只显示师徒；
2. `夫妻 + 同事 + 朋友` → 夫妻优先，朋友折叠；
3. 两个 hard 可共存；
4. 只有 soft 时仍显示最核心一个；
5. Display Policy 不修改 ledger。

### 19.5 Exception Resolver

1. 没有 high-impact case → 0 模型调用；
2. 20 普通 pending → 0 ExceptionCase；
3. batch 不携带上一批聊天历史；
4. 模型引用未知 ID → validator reject；
5. unauthorized action → reject；
6. unresolved → 不自动发起第二轮调查。

### 19.6 单章重跑

1. 只重新抽取目标章；
2. 但全书 identity/relation postprocess 重建；
3. manual edit 保留；
4. 自动 patch 不叠加；
5. staging 失败不污染 live；
6. 旧章因新 identity merge 需要 remap 时结果正确更新。

---

## 20. 实施顺序

### Phase 0 —— 固定当前基线

在当前已提交 Git 基线上：

- 保留现有全量测试；
- 固定《悉达多》raw extraction fixture；
- 保存当前 evaluator 报告；
- 增加分阶段 requests/tokens/characters/cases 统计。

完成条件：

> 能稳定比较旧后处理和新后处理，而不是每次重新抽取导致输入变化。

### Phase 1 —— 先移除错误的 pending → suspect 语义

立即修改：

```text
pending relation
→ pending review state
```

不再：

```text
pending relation
→ MissingEvidence suspect
```

保留旧 Reconcile 路径仅用于 feature flag 对照。

完成条件：20 条普通 pending 不产生 Final case。

### Phase 2 —— Identity Pipeline

实现：

```text
Identity Candidate Compiler
→ connected components
→ Identity Resolver
→ validator
→ deterministic merge
```

先解决对最终图影响最大的“同人裂点”。

完成条件：《悉达多》中的乔达摩类 identity 问题可通过自动流程处理，不依赖用户。

### Phase 3 —— 全书 Relation Cluster + Semantic Resolver

替换按章 normalization 主路径。

实现：

```text
all occurrences
→ exact clusters
→ candidate retrieval
→ batched semantic decisions
→ broadcast
```

同时扩充 seed registry 并加入 display tier。

### Phase 4 —— 全书 Evidence Resolver

实现：

```text
collect cases
→ merge overlapping context windows
→ bounded batches
→ distribute verdicts
```

不追求“所有书强制一次请求”。

### Phase 5 —— Canonical Facts + Display Policy

正式把：

```text
事实层
```

与：

```text
默认展示层
```

分开。

这一步完成后，大部分“关系过多”的问题不需要任何 LLM 修复。

### Phase 6 —— High-impact Anomaly / Exception Resolver

只实现小范围异常处理。

默认：

```text
0 cases → 0 request
```

不再开发通用 Final Reconcile Agent。

### Phase 7 —— 单章重跑改为全书后处理重建

保留 extraction 增量，但 postprocess 全书重算。

所有操作继续在 staging 中完成。

### Phase 8 —— 真实评测后删除 legacy

在：

```text
短：《悉达多》
中：《画像》
长：《白痴》
```

上比较：

- 产品质量；
- 默认图可读性；
- identity；
- relation recall；
- tokens；
- requests；
- wall time。

达到门槛后删除旧 tool Reconcile Agent。

---

## 21. Feature Flag 与回滚

迁移期间：

```text
RELATION_PIPELINE_VERSION=v2
IDENTITY_PIPELINE_VERSION=v2
EXCEPTION_RESOLVER_ENABLED=true
```

旧路径短期保留：

```text
RELATION_PIPELINE_VERSION=v1
RECONCILE_MODE=legacy_agent
```

只用于 A/B 与紧急回滚，不再继续向 legacy 增加功能。

---

## 22. 可观测性

日志 / 前端诊断至少输出：

```text
人物总数
identity candidate edges
identity components
identity model cases
identity merges

relation occurrences
exact semantic clusters
registry-resolved clusters
model semantic clusters
new predicates

unique evidence windows
evidence cases
confirmed / pending / rejected

canonical facts
hard / mid / soft fact counts
default-visible labels
hidden soft labels

high-impact exception cases
exception requests

每阶段 requests
每阶段 input/output tokens
每阶段 wall time
```

理想日志示例：

```text
Identity: 17 persons
→ 2 candidate components
→ 1 deterministic
→ 1 model case
→ 1 merge

Relations: 61 occurrences
→ 19 semantic clusters
→ 15 registry/exact
→ 4 model clusters
→ 1 request

Evidence: 61 occurrences
→ 14 unique context windows
→ 51 model cases
→ 2 requests
→ 44 confirmed / 5 pending / 2 rejected

Display: 31 canonical facts
→ 9 hard / 11 mid / 11 soft
→ 21 default-visible labels
→ 10 soft labels folded

Exceptions: 0 cases
→ 0 requests
```

看到这类数据，才能判断系统“为什么调用模型”。

---

## 23. 性能与产品验收定义

### P0：架构

- [ ] 不存在 Final Reconcile tool-calling loop；
- [ ] 普通 pending 不生成 Final case；
- [ ] identity 候选按 component 聚类；
- [ ] 相同 relation semantic 跨章只决策一次；
- [ ] Evidence context window 不无意义重复搬运；
- [ ] 默认没有 ExceptionCase 时为 0 次 Final 调用；
- [ ] LLM 不能主动扩大调查范围；
- [ ] 所有模型输出只能引用服务端稳定 ID；
- [ ] Patch 应用前有统一 validator；
- [ ] 单章 rerun 重建全书 postprocess；
- [ ] manual edits 与 staging/transaction 语义保留。

### P0：产品质量

- [ ] 默认关系图尽量覆盖主要人物和主要关系；
- [ ] 用户不需要处理 identity merge 才能得到可用图；
- [ ] wrong person merge = 0（Gold 覆盖范围内）；
- [ ] Required relation recall 不低于当前 Gold 基线，并以提升为目标；
- [ ] Forbidden violation 不得因追求 recall 明显上升；
- [ ] hard relation 不会被 soft relation 覆盖；
- [ ] soft relation 被压制时仍可在详情中查看；
- [ ] unresolved 不得伪装 confirmed。

### P0：成本

《悉达多》后处理：

- [ ] 正常无 Final Exception LLM 调用；
- [ ] 正常总后处理约 1～4 次模型请求；
- [ ] input tokens < 100k 为目标；
- [ ] > 150k 触发架构告警；
- [ ] > 200k 视为性能回归；
- [ ] 不存在跨请求累积对话历史。

### P1：耗时

在服务商正常延迟条件下：

```text
《悉达多》后处理 p50 < 30s
《悉达多》后处理 p95 < 90s
```

这只是后处理目标，不包含 Chapter Agent 全书抽取。

---

## 24. 本次明确不做

- 不引入向量数据库；
- 不建立重量级关系 ontology；
- 不让模型读整本书做 Final 总结式纠错；
- 不让用户成为必需的 merge / pending 审核员；
- 不因为某条关系弱就从事实账本删除；
- 不把所有文学关系提前穷举成 enum；
- V2 第一版不做复杂的增量 postprocess invalidation；
- 不用“模型自报 confidence”代替服务端校验；
- 不为了达到固定请求数而拼接巨型 prompt。

---

## 25. 最终结论

旧系统的问题不是“Reconcile Agent 不够聪明”，而是它承担了不该由 Agent 承担的职责。

ZhiYing 真正需要的是：

```text
自动认人
+ 尽量完整地记录关系
+ 全书级语义去重
+ 自动证据验证
+ 强弱关系展示收敛
+ 极少量高影响异常修复
```

而不是：

```text
抽取一次
→ 最后再让另一个 Agent 调查整本书并重做判断
```

因此 V2 的最终架构原则是：

> **账本尽量完整，图面主动收敛；自动解决身份问题；普通不确定性在所属阶段解决；只有真正会污染最终图的少量异常才进入一次性 Exception Resolver。**

如果这套架构实现正确，《悉达多》这种规模的作品不应该再需要几十次后处理模型调用，更不应该消耗百万级重复 Token。
