# ZhiYing 文档工作流

本文件只约束项目文档的使用方式。代码实现、编码风格、测试命令等不在这里规定。

## 1. 文档职责

### `docs/PRD.md` —— 产品需求 SSOT

`PRD.md` 只回答“产品要做什么、为什么做、用户是谁、功能如何取舍”。

所有涉及以下内容的决策都必须写入 PRD：

- 新功能与用户价值；
- 用户操作流程；
- 功能范围与非目标；
- 自动化程度与人工介入程度；
- 产品层面的准确率 / 完整性 / 性能取舍；
- 会改变用户最终看到什么的规则。

PRD 不应记录临时实现细节，也不应被某次代码实现反向绑死。

### `docs/ARCHITECTURE.md` —— 当前架构 SSOT

`ARCHITECTURE.md` 只描述“当前已经采用的技术架构和长期有效的工程约束”。

当代码完成一次架构级变更后，应同步更新这里，使它与真实实现一致。

不要把尚未实施的大型方案长期堆进 `ARCHITECTURE.md`；实施前的方案放到 `docs/design/`。

### `docs/human/` —— 给人看的说明

写法以 `docs/human/agent.md` 为准。这里的文档必须让人类维护者读得懂。

### `docs/design/` —— 临时设计文档

`docs/design/` 专门存放大范围代码变更开始前的临时设计方案。

设计文档只服务于一次具体实施，不是长期 SSOT。

代码完成、测试通过后：

1. 将仍然有效的产品决策合并回 `docs/PRD.md`；
2. 将已经落地的架构事实合并回 `docs/ARCHITECTURE.md`；
3. 删除本次对应的 `docs/design/*.md`，不要长期保留已经完成的设计稿。

---

## 2. 新功能的强制流程

如果任务属于**新功能**，在开始设计或写代码前必须先阅读 `docs/PRD.md`。

然后必须先向用户连续确认几个关键问题，不能直接根据自己的假设实现。问题应围绕真正影响产品取舍的内容，例如：

- 目标用户是谁、实际使用场景是什么；
- 用户希望自动完成到什么程度；
- 哪些结果宁可漏、哪些结果宁可多；
- 哪些信息应该默认展示，哪些只需保留在详情中；
- 性能、准确率、完整性、交互复杂度之间如何取舍；
- 什么属于本次范围，什么明确不做。

通常至少确认 3 个有实际取舍意义的问题；如果答案仍然存在关键歧义，应继续追问，直到能够明确产品行为。

确认完成后，**必须先把最终产品决策写入 `docs/PRD.md`，再开始代码设计或实现。**

不能只在聊天中形成共识而不更新 PRD。

---

## 3. 大范围代码变更的强制流程

如果一次代码修改会明显跨越多个模块、改变核心数据流、数据模型、Agent 流程、存储结构或主要 API，应视为**大范围变更**。

在开始大范围修改代码之前，必须先在：

```text
docs/design/
```

新建设计文档，例如：

```text
docs/design/relation-pipeline-v2.md
```

设计文档至少应说明：

- 当前问题与根因；
- 本次目标与非目标；
- 当前流程与目标流程；
- 核心数据结构 / API / 状态变化；
- 模块修改范围；
- 正确性与性能风险；
- 测试与验收标准；
- 实施顺序；
- 回滚或兼容策略（如果需要）。

设计确定后再进行大范围代码修改。

如果实施过程中设计发生实质变化，应先同步修改设计文档，再继续修改代码，避免代码和方案分叉。

---

## 4. 设计文档的生命周期

`docs/design/` 是临时工作区，不是历史档案馆。

一个大型改动完成后，必须进行文档收尾：

```text
产品决策      → PRD.md
已落地架构    → ARCHITECTURE.md
临时设计稿    → 删除
```

不要为了“留历史”把已经完成的设计稿继续留在 `docs/design/`。历史由 Git 提供。

如果代码没有完成，则对应 design 文档可以继续保留，作为未完成工作的恢复入口。

---

## 5. 判断原则

遇到不确定该写在哪里时：

```text
用户最终想要什么？          → PRD.md
系统现在实际上怎么工作？    → ARCHITECTURE.md
要给人看懂现在怎么跑？      → docs/human/
这次准备怎么大改、还没落地？ → docs/design/
```

始终避免为同一主题长期维护多份互相冲突的文档。





一个flow:

我目前打算：1.做crap工具箱并封装成cli(暂时只做Java的和python的)2.抄架构view和editor两个项目并做java和python版本的实现器 3.学习gherkin 4.学习验收测试，变异测试 5.把crap,测试，架构做成ci门禁 

以这个项目为例尝试搞可维护的agent主导的项目
1.精简agent.md到极限
2.确立注释规范:函数级别必须注释，函数如果内部逻辑块cc（代码复杂度）大于6必须注释
3.确立门禁:函数cc必须小于等于12，一旦超过则需要重构
4.确定测试:函数必须对其行为要有单元测试，测试覆盖率要高
   但是必须对每一个代码要有使用Gherkin做的验收测试
   尝试学着做变异测试（不止做源码变异，也修改 Gherkin 里的 Examples 数据，然后重新跑验收测试）
5.在编码之前要有架构检查器，让用户时刻知道架构
6.尝试敏捷开发，在完成prd后，一次只选几个故事，去做几个Gherkin，做完再做下一个
